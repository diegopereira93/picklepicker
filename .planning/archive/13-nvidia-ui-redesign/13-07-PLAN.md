---
phase: 13-nvidia-ui-redesign
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/header.tsx
autonomous: true
gap_closure: true
requirements:
  - HY-06
  - HY-11 (partial - header.tsx)
must_haves:
  truths:
    - "Navigation logo has lime accent on 'IQ' portion"
    - "Header component uses hy-* class prefix instead of nv-*"
  artifacts:
    - path: "frontend/src/components/layout/header.tsx"
      provides: "Navigation with lime-accented logo and migrated classes"
      contains: "Pickle<span>IQ</span>"
  key_links:
    - from: "header.tsx"
      to: "globals.css"
      via: "className hy-* classes"
      pattern: "hy-nav|hy-container|hy-nav-brand"
---

<objective>
Fix navigation logo to have lime accent on "IQ" and migrate header.tsx to hy-* class prefix.

Purpose: The Hybrid design spec requires the logo to render as "PickleIQ" with the "IQ" portion highlighted in lime (#84CC16). Additionally, HY-11 requires migrating nv-* classes to hy-* classes. This plan addresses both gaps in header.tsx.
Output: header.tsx with lime-accented logo and hy-* class names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-nvidia-ui-redesign/CONTEXT.md

<interfaces>
<!-- From CONTEXT.md HY-06 specification -->
- Logo: left-aligned, "PickleIQ" with `<span>` in lime

<!-- From globals.css -->
```css
.hy-nav-brand span {
  color: var(--sport-primary);  /* #84CC16 lime */
}
```

<!-- Class migration mapping -->
nv-nav -> hy-nav
nv-container -> hy-container
nv-nav-brand -> hy-nav-brand
nv-nav-links -> hy-nav-links
nv-nav-link -> hy-nav-link
nv-nav-cta -> hy-nav-cta
nv-nav-mobile -> hy-nav-mobile
nv-nav-overlay -> hy-nav-overlay
nv-nav-link-mobile -> hy-nav-link-mobile
nv-nav-logo -> hy-nav-logo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lime accent to logo and migrate header classes</name>
  <files>frontend/src/components/layout/header.tsx</files>
  <read_first>
    - frontend/src/components/layout/header.tsx (current state with plain logo and nv-* classes)
    - .planning/phases/13-nvidia-ui-redesign/CONTEXT.md (HY-06 and HY-11 specifications)
    - frontend/src/app/globals.css (hy-* class definitions and .hy-nav-brand span rule)
  </read_first>
  <behavior>
    - Logo renders as "Pickle" + "IQ" where "IQ" has lime color
    - All nv-* class names replaced with hy-* equivalents
    - CSS .hy-nav-brand span rule applies lime color to the IQ span
  </behavior>
  <action>
    Modify frontend/src/components/layout/header.tsx:

    **Part A: Logo Lime Accent**
    Find ALL occurrences of the logo text and update them:
    1. Line ~100: Change `<span className="nv-nav-brand">PickleIQ</span>` to:
       `<span className="hy-nav-brand">Pickle<span>IQ</span></span>`

    2. Line ~135: Change `<Link ... className="nv-nav-brand">PickleIQ</Link>` to:
       `<Link ... className="hy-nav-brand">Pickle<span>IQ</span></Link>`

    **Part B: Class Migration**
    Replace ALL nv-* class names with hy-* equivalents:
    - `nv-nav` -> `hy-nav`
    - `nv-container` -> `hy-container`
    - `nv-nav-logo` -> `hy-nav-logo`
    - `nv-nav-brand` -> `hy-nav-brand`
    - `nv-nav-links` -> `hy-nav-links`
    - `nv-nav-link` -> `hy-nav-link`
    - `nv-nav-cta` -> `hy-nav-cta`
    - `nv-nav-mobile` -> `hy-nav-mobile`
    - `nv-nav-overlay` -> `hy-nav-overlay`
    - `nv-nav-link-mobile` -> `hy-nav-link-mobile`

    Use sed or manual replacement to ensure ALL occurrences are updated.
  </action>
  <acceptance_criteria>
    - grep 'Pickle<span>IQ</span>' frontend/src/components/layout/header.tsx returns 2 matches (desktop and mobile logos)
    - grep 'nv-' frontend/src/components/layout/header.tsx returns ZERO matches
    - grep 'hy-nav-brand' frontend/src/components/layout/header.tsx returns at least 2 matches
    - grep 'hy-nav\\|hy-container\\|hy-nav-link' frontend/src/components/ui/header.tsx returns multiple matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Pickle<span>IQ</span>' frontend/src/components/layout/header.tsx | xargs -I {} test {} -eq 2 && grep -c 'nv-' frontend/src/components/layout/header.tsx | xargs -I {} test {} -eq 0 && echo "PASS"</automated>
  </verify>
  <done>
    - Logo has Pickle<span>IQ</span> structure in both desktop and mobile views
    - All nv-* classes replaced with hy-* equivalents
    - CSS .hy-nav-brand span rule applies lime to "IQ"
  </done>
</task>

</tasks>

<verification>
- [ ] Logo "IQ" portion renders in lime color
- [ ] No nv-* classes remain in header.tsx
- [ ] All hy-* classes correctly applied
</verification>

<success_criteria>
- grep confirms Pickle<span>IQ</span> structure
- grep confirms no nv-* classes remain
- Visual: "IQ" in logo appears in lime green
</success_criteria>

<output>
After completion, create `.planning/phases/13-nvidia-ui-redesign/13-07-SUMMARY.md`
</output>