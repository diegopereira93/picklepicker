---
phase: 13-nvidia-ui-redesign
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/layout.tsx
autonomous: true
gap_closure: true
requirements:
  - HY-01
must_haves:
  truths:
    - "Google Fonts loaded for Hybrid typography (Instrument Sans, Inter, JetBrains Mono)"
    - "CSS font variables applied to html/body, not inline styles"
  artifacts:
    - path: "frontend/src/app/layout.tsx"
      provides: "Google Fonts CDN links and font application"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "layout.tsx"
      to: "Google Fonts CDN"
      via: "<link> tags in <head>"
      pattern: "fonts.googleapis.com/css2"
---

<objective>
Fix typography system to load Hybrid fonts via Google Fonts CDN instead of NVIDIA-EMEA inline font-family.

Purpose: The Hybrid Modern Sports Tech design requires Instrument Sans (display), Inter (body), and JetBrains Mono (data/specs). Currently layout.tsx uses NVIDIA-EMEA font which blocks the CSS font variables from applying.
Output: layout.tsx with Google Fonts CDN links and proper font variable usage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-nvidia-ui-redesign/CONTEXT.md

<interfaces>
<!-- Key CSS variables from globals.css that layout.tsx must NOT override -->
From frontend/src/app/globals.css:
```css
--font-display: 'Instrument Sans', 'Inter', system-ui, sans-serif;
--font-body: 'Inter', system-ui, -apple-system, sans-serif;
--font-data: 'JetBrains Mono', 'Fira Code', monospace;
```

<!-- Existing globals.css base rule -->
```css
html {
  font-family: var(--font-body);
  background-color: var(--color-black);
  color: var(--color-white);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google Fonts CDN and remove inline font override</name>
  <files>frontend/src/app/layout.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx (current state with NVIDIA-EMEA inline style)
    - .planning/phases/13-nvidia-ui-redesign/CONTEXT.md (HY-01 specification)
  </read_first>
  <behavior>
    - Google Fonts CDN links present for Instrument Sans, Inter, JetBrains Mono
    - Inline fontFamily style removed from html element
    - Body element uses CSS classes that inherit font from globals.css
  </behavior>
  <action>
    Modify frontend/src/app/layout.tsx:

    1. In the `<head>` element, add Google Fonts CDN links BEFORE the Font Awesome link:
    ```html
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Instrument+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
    ```

    2. Remove the inline `style={{ fontFamily: "'NVIDIA-EMEA', Arial, Helvetica, sans-serif" }}` from the `<html>` element entirely. The html element should only have `lang="pt-BR"`.

    3. The `<body>` element already has className "min-h-screen bg-background text-foreground antialiased flex flex-col" which will inherit font from globals.css.

    Do NOT modify the Font Awesome link - keep it as-is.
  </action>
  <acceptance_criteria>
    - grep 'fonts.googleapis.com' frontend/src/app/layout.tsx returns the Google Fonts CDN URL
    - grep 'fonts.gstatic.com' frontend/src/app/layout.tsx returns the preconnect link
    - grep 'Instrument+Sans' frontend/src/app/layout.tsx returns the font-family parameter
    - grep 'JetBrains+Mono' frontend/src/app/layout.tsx returns the font-family parameter
    - grep "NVIDIA-EMEA" frontend/src/app/layout.tsx returns NO matches
    - grep "style=" frontend/src/app/layout.tsx on the html element returns NO matches
  </acceptance_criteria>
  <verify>
    <automated>grep -E "fonts.googleapis.com|fonts.gstatic.com|Instrument+Sans|JetBrains+Mono" frontend/src/app/layout.tsx && ! grep "NVIDIA-EMEA" frontend/src/app/layout.tsx</automated>
  </verify>
  <done>
    - Google Fonts CDN links present in layout.tsx head
    - NVIDIA-EMEA inline font removed
    - HTML element no longer has inline style attribute
    - Font inheritance flows from globals.css CSS variables
  </done>
</task>

</tasks>

<verification>
- [ ] Google Fonts CDN loads Instrument Sans, Inter, JetBrains Mono
- [ ] No inline font-family override on html element
- [ ] CSS font variables from globals.css apply correctly
</verification>

<success_criteria>
- grep confirms Google Fonts CDN URLs present
- grep confirms NVIDIA-EMEA removed
- Visual: body text renders with Inter font, not NVIDIA-EMEA
</success_criteria>

<output>
After completion, create `.planning/phases/13-nvidia-ui-redesign/13-05-SUMMARY.md`
</output>