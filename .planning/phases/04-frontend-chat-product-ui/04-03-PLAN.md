---
phase: 04-frontend-chat-product-ui
plan: 04-03
type: execute
wave: 2
autonomous: true
depends_on: ["04-01"]
blocks: []
files_modified:
  - frontend/src/app/compare/page.tsx
  - frontend/src/components/compare/paddle-search.tsx
  - frontend/src/components/compare/comparison-table.tsx
  - frontend/src/components/compare/radar-chart-wrapper.tsx
  - frontend/src/components/compare/paddle-selector.tsx
  - frontend/src/tests/unit/radar-chart.test.ts
  - frontend/src/tests/unit/comparison-table.test.ts
  - frontend/src/tests/unit/paddle-search.test.ts
requirements:
  - R4.3
estimated_tokens: 35000

must_haves:
  truths:
    - "User can search paddles by name with autocomplete dropdown"
    - "User can select 2-3 paddles for side-by-side comparison"
    - "Comparison table shows specs + translated descriptions side-by-side"
    - "RadarChart renders without SSR hydration errors (dynamic import with ssr:false)"
    - "Each paddle shows lowest price and buy link"
    - "Removing a paddle from comparison updates table and chart"
  artifacts:
    - path: "frontend/src/components/compare/paddle-search.tsx"
      provides: "Autocomplete search with debounced API calls"
      min_lines: 50
    - path: "frontend/src/components/compare/comparison-table.tsx"
      provides: "Side-by-side specs table with metric translations"
      min_lines: 60
    - path: "frontend/src/components/compare/radar-chart-wrapper.tsx"
      provides: "Recharts RadarChart with ssr:false dynamic import"
      min_lines: 40
    - path: "frontend/src/app/compare/page.tsx"
      provides: "Comparison page orchestrating search, table, chart"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/compare/paddle-search.tsx"
      to: "frontend/src/lib/api.ts"
      via: "searchPaddles for autocomplete"
      pattern: "searchPaddles|fetchPaddles"
    - from: "frontend/src/components/compare/radar-chart-wrapper.tsx"
      to: "recharts"
      via: "dynamic import ssr:false"
      pattern: "dynamic.*ssr.*false"
    - from: "frontend/src/components/compare/comparison-table.tsx"
      to: "frontend/src/types/paddle.ts"
      via: "Paddle + PaddleSpecs types"
      pattern: "PaddleSpecs|Paddle"
---

<objective>
Build the paddle comparison page with search/autocomplete, side-by-side specs table with metric translations, and radar chart visualization. Users select 2-3 paddles and see a clear visual and tabular comparison.

Purpose: The comparison page serves the analytical intermediate player persona who wants data-driven decisions. It drives affiliate clicks by showing the lowest price per paddle with buy links.
Output: /compare page with search, table, radar chart, and price/buy links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-chat-product-ui/CONTEXT.md
@.planning/phases/04-frontend-chat-product-ui/04-01-SUMMARY.md

<interfaces>
<!-- From 04-01: shared types and API client -->
From frontend/src/types/paddle.ts:
```typescript
export interface PaddleSpecs {
  swingweight?: number;
  twistweight?: number;
  weight_oz?: number;
  grip_size?: string;
  core_thickness_mm?: number;
  face_material?: string;
}

export interface Paddle {
  id: number;
  name: string;
  brand: string;
  sku?: string;
  image_url?: string;
  specs?: PaddleSpecs;
  price_min_brl?: number;
  created_at: string;
}
```

From frontend/src/lib/api.ts:
```typescript
export async function fetchPaddles(params?): Promise<PaddleListResponse>;
export async function fetchPaddle(id: number): Promise<Paddle>;
export async function fetchLatestPrices(paddleId: number): Promise<LatestPriceResponse>;
export async function searchPaddles(query: string, limit?: number): Promise<Paddle[]>;
```

<!-- Metric translation rules from R3.3 -->
Swingweight < 100 -> "Raquete agil — ideal para reacoes rapidas na rede"
Swingweight > 120 -> "Mais potencia — ideal para fundo de quadra"
Twistweight < 6 -> "Exige precisao — para jogadores experientes"
Twistweight > 7 -> "Sweet spot ampliado — perdoa erros de posicionamento"
Core 13mm -> "Resposta viva e potencia — para jogadores agressivos"
Core 16mm -> "Mais controle e absorcao — indicado para iniciantes"
Face carbono T700 -> "Durabilidade premium com spin consistente"
Face fibra de vidro -> "Toque suave e forgiving — otimo para iniciantes"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paddle search autocomplete + comparison table with metric translations</name>
  <files>
    frontend/src/components/compare/paddle-search.tsx,
    frontend/src/components/compare/paddle-selector.tsx,
    frontend/src/components/compare/comparison-table.tsx,
    frontend/src/lib/metric-translations.ts,
    frontend/src/tests/unit/paddle-search.test.ts,
    frontend/src/tests/unit/comparison-table.test.ts
  </files>
  <behavior>
    - Test 1: PaddleSearch shows dropdown with results after typing 2+ characters
    - Test 2: PaddleSearch debounces API calls (300ms) — single call for rapid typing
    - Test 3: Selecting a paddle from dropdown adds it to comparison (max 3)
    - Test 4: Selecting a 4th paddle is blocked with "Maximo 3 raquetes" message
    - Test 5: ComparisonTable renders spec rows (swingweight, twistweight, core, face, weight) for 2 paddles
    - Test 6: ComparisonTable shows translated description alongside raw value
    - Test 7: translateMetric("swingweight", 95) returns "Raquete agil — ideal para reacoes rapidas na rede"
    - Test 8: ComparisonTable shows price and "Comprar" link per paddle
    - Test 9: Removing a paddle updates the table
  </behavior>
  <action>
    1. Create `frontend/src/lib/metric-translations.ts`:
       - `translateMetric(metric: string, value: number | string): string` implementing the R3.3 translation rules:
         - swingweight: <100 "agil", 100-120 "equilibrada", >120 "potencia"
         - twistweight: <6 "exige precisao", 6-7 "bom equilibrio", >7 "sweet spot ampliado"
         - core_thickness_mm: <=14 "resposta viva", 14-15 "versatil", >=16 "mais controle"
         - face_material: contains "carbon"/"T700" -> "durabilidade premium", contains "fiberglass"/"fibra" -> "toque suave"
       - Export mapping for RadarChart dimension labels

    2. Create `frontend/src/components/compare/paddle-search.tsx`:
       - Controlled input with shadcn Input component
       - Debounced search: 300ms delay, min 2 characters, calls `searchPaddles(query, 8)`
       - Dropdown positioned below input (absolute positioning)
       - Each result shows: paddle name, brand, price_min_brl (if available)
       - Click result -> fires `onSelect(paddle: Paddle)` callback
       - Click outside or Escape -> close dropdown
       - Loading state with Skeleton inside dropdown
       - Empty state: "Nenhuma raquete encontrada"

    3. Create `frontend/src/components/compare/paddle-selector.tsx`:
       - Shows 2-3 PaddleSearch slots (always show at least 2, optionally 3rd)
       - Each slot shows selected paddle name + "X" remove button, or empty PaddleSearch
       - "Adicionar raquete" button to show 3rd slot (hidden until 2 selected)
       - Max 3 paddles enforced
       - Props: `{ paddles: Paddle[], onAdd: (p: Paddle) => void, onRemove: (id: number) => void }`

    4. Create `frontend/src/components/compare/comparison-table.tsx`:
       - Props: `{ paddles: Paddle[] }` (2-3 paddles)
       - Table columns: Spec label | Paddle 1 | Paddle 2 | (Paddle 3)
       - Rows for each spec: Swingweight, Twistweight, Peso (oz), Core (mm), Face Material, Grip
       - Each cell shows: raw value + translated description in muted text below
       - If spec is null/undefined: show "Nao disponivel" in muted text
       - Bottom row: Menor preco R$ + "Comprar" link (affiliate URL or retailer link)
       - Responsive: horizontal scroll on mobile, sticky first column (spec labels)

    5. Write tests covering all behaviors listed above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/unit/paddle-search.test.ts src/tests/unit/comparison-table.test.ts</automated>
  </verify>
  <done>Search autocomplete works with 300ms debounce and max 3 paddles. Comparison table shows all specs with PT-BR translations alongside raw values. Price + buy link per paddle. All 9 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RadarChart visualization + comparison page assembly</name>
  <files>
    frontend/src/components/compare/radar-chart-wrapper.tsx,
    frontend/src/app/compare/page.tsx,
    frontend/src/tests/unit/radar-chart.test.ts
  </files>
  <behavior>
    - Test 1: RadarChart component does not crash on server-side render (ssr:false guard verified)
    - Test 2: RadarChart renders 5 axes: Potencia, Controle, Manobrabilidade, Sweet Spot, Custo-Beneficio
    - Test 3: RadarChart accepts 2-3 paddle datasets with different colors
    - Test 4: Compare page renders search + table + chart when 2+ paddles selected
    - Test 5: Compare page shows empty state prompt when <2 paddles selected
  </behavior>
  <action>
    1. Create `frontend/src/components/compare/radar-chart-wrapper.tsx`:
       - Use `dynamic(() => import('./radar-chart-inner'), { ssr: false })` pattern
       - Create inner component `radar-chart-inner.tsx` that imports from recharts:
         - `RadarChart`, `PolarGrid`, `PolarAngleAxis`, `PolarRadiusAxis`, `Radar`, `Legend`, `ResponsiveContainer`
       - 5 axes derived from paddle specs:
         - **Potencia**: normalize swingweight (80-140 range -> 0-100)
         - **Controle**: inverse of core_thickness (13mm=40, 16mm=90) + twistweight factor
         - **Manobrabilidade**: inverse of weight_oz (6oz=90, 9oz=30)
         - **Sweet Spot**: normalize twistweight (4-9 range -> 0-100)
         - **Custo-Beneficio**: inverse of price_min_brl (R$200=90, R$1500=20)
       - Each paddle gets a colored Radar polygon (blue, green, orange)
       - Legend shows paddle names with colors
       - Props: `{ paddles: Paddle[] }`
       - Responsive: `<ResponsiveContainer width="100%" height={400}>`
       - Handle missing specs gracefully (default to 50 for null values)

    2. Create `frontend/src/app/compare/page.tsx`:
       - State: `selectedPaddles: Paddle[]` (max 3)
       - Layout (top to bottom):
         - Page title: "Comparar Raquetes"
         - PaddleSelector (search + selected paddles)
         - If 2+ paddles: ComparisonTable + RadarChartWrapper side-by-side on desktop, stacked on mobile
         - If <2 paddles: empty state card with instructions "Selecione pelo menos 2 raquetes para comparar"
       - URL state: store paddle IDs in query params `?ids=1,2,3` for shareability
       - On mount: if query params have ids, fetch paddles and populate selection
       - Page metadata: title "Comparar Raquetes - PickleIQ"

    3. Write radar-chart test that verifies:
       - Component renders without SSR crash (mock dynamic import)
       - Renders with mock paddle data (2 paddles with full specs)
       - Handles paddles with missing specs (no crash, uses defaults)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/unit/radar-chart.test.ts && npm run build</automated>
  </verify>
  <done>RadarChart renders 5 axes without SSR hydration errors. Comparison page shows search + table + chart for 2+ paddles. URL query params preserve selection. Build succeeds with dynamic import. All tests pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run` -- all comparison tests pass
2. `npm run build` succeeds (no SSR hydration issues with dynamic import)
3. /compare page: search works, selecting 2 paddles shows table + chart
4. RadarChart renders 5 axes with colored polygons per paddle
5. Metric translations show PT-BR descriptions alongside raw values
6. Max 3 paddles enforced in selector
7. URL query params preserve paddle selection on page reload
</verification>

<success_criteria>
- Autocomplete search returns results after 2+ characters with 300ms debounce
- Max 3 paddles selectable, 4th blocked with message
- Comparison table shows all specs with PT-BR metric translations
- RadarChart renders 5 axes (Potencia, Controle, Manobrabilidade, Sweet Spot, Custo-Beneficio)
- No SSR hydration errors (dynamic import with ssr:false)
- Each paddle shows lowest price + buy link
- URL query params preserve selection
- Responsive: table scrolls horizontally on mobile, chart stacks below table
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-chat-product-ui/04-03-SUMMARY.md`
</output>
