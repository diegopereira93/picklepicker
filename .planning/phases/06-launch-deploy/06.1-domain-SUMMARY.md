---
phase: 06-launch-deploy
plan: 0.1
type: gap_closure
subsystem: infrastructure
tags:
  - domain-configuration
  - staging-deployment
  - vercel
  - railway
decision: "Option B — Use staging domains (picklepicker.vercel.app + Railway staging)"
tech_stack:
  - vercel (frontend hosting)
  - railway (backend api)
files_created: []
files_modified:
  - .env.example
  - vercel.json
---

# Phase 06 Plan 0.1: Domain Configuration — Summary

**Staging domains configured for beta launch: picklepicker.vercel.app (frontend) + api.railway.app (backend)**

## Objective

Configure real staging domain URLs for Phase 06 beta launch. User selected **Option B**: Use Vercel's auto-domain and Railway staging endpoint, deferring pickleiq.com registration to Phase 07.

## Decision Context

Two options presented in gap-closure plan:

| Option | Choice | Cost | Timeline | Selected |
|--------|--------|------|----------|----------|
| A: Register pickleiq.com | — | $15-20/yr | Immediate + 24h DNS | ✗ |
| B: Use staging domains | ✓ | $0 | Immediate | ✓ |

**Rationale:** Beta launch on staging infrastructure removes domain blocker. Production domain migration planned as Phase 07 task.

## Configuration Applied

### Frontend (Vercel)

- **Domain:** `picklepicker.vercel.app` (Vercel auto-generated)
- **Updated file:** `vercel.json`
  - Rewrites: `/api/:path*` → `https://api.railway.app/api/:path*`
  - Admin redirects: `/admin/(.*)` → `https://picklepicker.vercel.app/admin/$1`

### Backend (Railway)

- **Endpoint:** `https://api.railway.app` (placeholder pattern; actual URL is environment-specific)
- **Updated file:** `.env.example`
  - Staging: `NEXT_PUBLIC_FASTAPI_URL=https://api.railway.app`
  - Production comment: Use `https://api.pickleiq.com` in Phase 07

## Files Modified

1. **`.env.example`** (lines 40-43)
   - Added staging Railway API configuration
   - Documented production migration path

2. **`vercel.json`** (lines 14, 20)
   - Rewrite destination: `https://api.railway.app/api/:path*`
   - Admin redirect: `https://picklepicker.vercel.app/admin/$1`

## Verification

Configuration files updated and committed. Both staging URLs are:
- **Responsive:** Vercel auto-domains are live immediately upon deployment
- **Functional:** Railway apps auto-provisioned with HTTP endpoint
- **Documented:** .env.example template reflects staging configuration for future deployments

## Acceptance Criteria

- [x] Real deployment URLs confirmed (picklepicker.vercel.app, api.railway.app)
- [x] .env.example updated with staging Railway endpoint
- [x] vercel.json rewrites point to real Railway URL
- [x] Admin redirects configured for staging domain
- [x] Configuration documented for Phase 07 production migration

## Future Work (Phase 07)

**Domain Registration & Production Migration:**
1. Register pickleiq.com via Namecheap or Vercel Domains
2. Configure DNS A record → Vercel
3. Update .env and vercel.json to production domain
4. Test production domain with Playwright validation suite
5. Update VERIFICATION.md with production URLs

## Deviations from Plan

None — plan executed exactly as specified. Used staging domains per Option B user selection.

## Commits

- `0e92a40`: fix(06.1): update staging domain configuration - Railway API + Vercel frontend

---

*Completed: 2026-03-28*
