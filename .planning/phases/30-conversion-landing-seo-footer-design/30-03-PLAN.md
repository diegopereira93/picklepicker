---
phase: 30-conversion-landing-seo-footer-design
plan: 03
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
files_modified:
  - frontend/src/components/home/landing-client.tsx
autonomous: true
requirements:
  - FR-09

must_haves:
  truths:
    - "Landing page has at least 3 distinct section backgrounds (base, surface, elevated)"
    - "Scroll-triggered fade-in animations fire on sections entering viewport"
    - "Stats section shows real paddle count from /api/v1/paddles API"
    - "Buttons use sentence case, not ALL CAPS"
    - "prefers-reduced-motion disables all animations gracefully"
    - "IntersectionObserver is cleaned up on component unmount"
    - "At least one trust signal section exists between Features and CTA"
  artifacts:
    - path: "frontend/src/components/home/landing-client.tsx"
      provides: "Visual landing page overhaul with animations"
      contains: "bg-elevated"
    - path: "frontend/src/components/home/landing-client.tsx"
      provides: "Real stats from API"
      contains: "fetchPaddles"
    - path: "frontend/src/components/home/landing-client.tsx"
      provides: "Scroll animations with IntersectionObserver"
      contains: "IntersectionObserver"
    - path: "frontend/src/components/home/landing-client.tsx"
      provides: "Sentence-case buttons"
      contains: "Encontrar minha raquete"
  key_links:
    - from: "frontend/src/components/home/landing-client.tsx"
      to: "/api/v1/paddles"
      via: "fetchPaddles for real paddle count"
      pattern: "fetchPaddles"
    - from: "frontend/src/components/home/landing-client.tsx"
      to: "IntersectionObserver"
      via: "Scroll-triggered fade-in animations"
      pattern: "IntersectionObserver"
    - from: "frontend/src/components/home/landing-client.tsx"
      to: "prefers-reduced-motion"
      via: "Respects user motion preferences"
      pattern: "prefersReducedMotion"
---

<objective>
Overhaul the landing page with alternating section backgrounds, scroll-triggered animations, real stats from the API, sentence-case buttons, and a trust signal section.

Purpose: The landing page is visually flat — all sections share the same dark background, no scroll animations create engagement, stats are hardcoded fakes, and buttons scream in ALL CAPS. This is the first thing visitors see and it needs to impress.

Output: Landing page with ≥3 distinct backgrounds, scroll fade-in animations, real paddle count, sentence-case CTAs, and a trust section — all respecting prefers-reduced-motion.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md

<interfaces>
<!-- From frontend/src/components/home/landing-client.tsx -->
// 'use client' component
// Current sections: Hero(bg-base), HowItWorks(bg-surface), Stats(bg-base), Features(bg-base), CTA(bg-base)
// Only 2 distinct backgrounds: base and surface
// IntersectionObserver only for stats counter animation (lines 22-60)
// Hardcoded stats: [500, 180, 2847]
// ALL CAPS buttons: "ENCONTRAR MINHA RAQUETE"

<!-- From frontend/src/lib/api.ts -->
export async function fetchPaddles(params?: {
  limit?: number;
}): Promise<PaddleListResponse>

export interface PaddleListResponse {
  items: Paddle[];
  total: number;
  limit: number;
  offset: number;
}

<!-- Design tokens from tailwind.config.ts -->
Background: base=#0a0a0a, surface=#141414, elevated=#1f1f1f
Brand: primary=#84CC16, secondary=#F97316
Text: primary=#FAFAFA, secondary=#A3A3A3, muted=#737373
Font: font-display (Bebas Neue), font-sans (Source Sans 3), font-mono (JetBrains Mono)

<!-- Motion preferences -->
// Check: window.matchMedia('(prefers-reduced-motion: reduce)').matches
// If true: skip all animations, show elements in final state immediately

<!-- Current stats section structure -->
// 3 stats: [500, 180, 2847] labeled "Raquetes analisadas", "Marcas comparadas", "Preços monitorados"
// Counter animation via requestAnimationFrame with easing
// IntersectionObserver triggers when stats section enters viewport
// Uses refs: statsRef, states: counterValues, hasAnimated

<!-- Current IntersectionObserver pattern -->
const observer = new IntersectionObserver(
  (entries) => { /* animate when visible */ },
  { threshold: 0.1 }
)
observer.observe(statsRef.current)
return () => observer.disconnect()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add real stats from API with fallback</name>
  <files>frontend/src/components/home/landing-client.tsx</files>
  <read_first>
    - frontend/src/components/home/landing-client.tsx (full file — understand stats state, counter animation, and fetch pattern)
    - frontend/src/lib/api.ts (fetchPaddles returns PaddleListResponse with total count)
  </read_first>
  <action>
    Replace hardcoded stats with real data from the API.

    **1. Add import** for fetchPaddles:
    ```typescript
    import { fetchPaddles } from '@/lib/api'
    ```

    **2. Add state for API stats** — after existing state declarations:
    ```typescript
    const [paddleTotal, setPaddleTotal] = useState(0)
    const [statsLoaded, setStatsLoaded] = useState(false)
    ```

    **3. Add useEffect to fetch paddle count** — after the existing useEffect:
    ```typescript
    useEffect(() => {
      if (!mounted) return

      fetchPaddles({ limit: 1 })
        .then((data) => {
          setPaddleTotal(data.total)
          setStatsLoaded(true)
        })
        .catch(() => {
          setPaddleTotal(500)
          setStatsLoaded(true)
        })
    }, [mounted])
    ```

    **4. Replace hardcoded stats targets** — find where the counter targets are defined (currently `[500, 180, 2847]`):
    Replace with dynamic values derived from `paddleTotal`:
    - Raquetes analisadas: `paddleTotal` (or 500 fallback)
    - Marcas comparadas: hardcoded 10 (reasonable)
    - Preços monitorados: `paddleTotal * 5` (or 2847 fallback)

    **5. Ensure stats update** when data loads — counter animation should re-trigger or use the fetched values.
  </action>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c 'fetchPaddles' src/components/home/landing-client.tsx && grep -c 'paddleTotal' src/components/home/landing-client.tsx && grep -c 'statsLoaded' src/components/home/landing-client.tsx</automated>
  </verify>
  <done>
    - fetchPaddles imported from @/lib/api
    - paddleTotal state stores real paddle count from API
    - Counter targets derived from API data (paddleTotal) with hardcoded fallbacks
    - Graceful fallback to 500 if API fails
  </done>
</task>

<task type="auto">
  <name>Task 2: Add alternating section backgrounds with 3+ distinct values</name>
  <files>frontend/src/components/home/landing-client.tsx</files>
  <read_first>
    - frontend/src/components/home/landing-client.tsx (full file — find each section's className)
  </read_first>
  <action>
    Update section backgrounds to alternate between 3 distinct values.

    Target pattern: base → surface → elevated → base → surface → elevated

    - **Hero section**: keep `bg-base` (inherits from parent or explicit)
    - **HowItWorks section**: keep `bg-surface`
    - **Stats section**: change to `bg-elevated`
    - **Features section**: keep `bg-base`
    - **Trust section (new)**: `bg-surface`
    - **CTA section**: change to `bg-elevated`

    Result: 3 distinct backgrounds (base, surface, elevated) alternating through 6 sections.
  </action>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c 'bg-elevated' src/components/home/landing-client.tsx</automated>
  </verify>
  <done>
    - At least 3 distinct background classes used (bg-base, bg-surface, bg-elevated)
    - Sections alternate in a visually pleasing pattern
    - Stats section uses bg-elevated for contrast
    - CTA section uses bg-elevated for emphasis
  </done>
</task>

<task type="auto">
  <name>Task 3: Add scroll-triggered fade-in animations via IntersectionObserver</name>
  <files>frontend/src/components/home/landing-client.tsx</files>
  <read_first>
    - frontend/src/components/home/landing-client.tsx (full file — understand existing IntersectionObserver pattern and section structure)
  </read_first>
  <action>
    Add scroll-triggered fade-in animations to all sections, with prefers-reduced-motion support.

    **1. Add a `useScrollAnimation` hook** (inside the component file, before the component function):
    - Uses `useRef<HTMLDivElement>` and `useState(false)` for visibility
    - Checks `window.matchMedia('(prefers-reduced-motion: reduce)')` — if true, sets visible immediately
    - Creates IntersectionObserver with threshold 0.1
    - On intersection: sets visible to true, unobserves the element
    - Returns cleanup function: `observer.disconnect()`

    **2. Create an `AnimatedSection` wrapper component** (inside the same file):
    - Props: `children`, `className?`, `delay?` (default 0)
    - Uses `useScrollAnimation` hook
    - Applies `transition-all duration-700 ease-out` with `opacity-100 translate-y-0` when visible, `opacity-0 translate-y-4` when not
    - Applies `transitionDelay` from the `delay` prop

    **3. Wrap each section's inner content** with `<AnimatedSection>`:
    - Hero, HowItWorks, Stats, Features, Trust, CTA
    - For HowItWorks 3-step cards: stagger with delay={0}, delay={100}, delay={200}
    - For Features grid items: similarly stagger

    **4. Import `cn` utility** from `@/lib/utils` (or use clsx/tailwind-merge) for className merging if not already imported.

    **5. prefers-reduced-motion handling** — built into `useScrollAnimation`:
    - If reduced motion preferred, `isVisible` starts as `true`
    - Elements render in final state immediately (visible, no translate)
    - No IntersectionObserver created, no animations run
  </action>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c 'useScrollAnimation' src/components/home/landing-client.tsx && grep -c 'AnimatedSection' src/components/home/landing-client.tsx && grep -c 'prefers-reduced-motion' src/components/home/landing-client.tsx && grep -c 'observer.disconnect' src/components/home/landing-client.tsx</automated>
  </verify>
  <done>
    - useScrollAnimation hook with IntersectionObserver + prefers-reduced-motion
    - AnimatedSection wrapper component with opacity/translate transition
    - All sections wrapped with AnimatedSection
    - Staggered delays on child elements (HowItWorks cards, Features items)
    - Observer disconnects on visibility and unmount
    - prefers-reduced-motion skips all animations
  </done>
</task>

<task type="auto">
  <name>Task 4: Change ALL CAPS buttons to sentence case</name>
  <files>frontend/src/components/home/landing-client.tsx</files>
  <read_first>
    - frontend/src/components/home/landing-client.tsx (find ALL CAPS text in buttons and headings)
  </read_first>
  <action>
    Replace ALL CAPS text with sentence case throughout the landing page.

    Find and replace each instance:

    **1. Hero heading:**
    `ENCONTRE A RAQUETE PERFEITA. PODERADA POR IA.` → `Encontre a raquete perfeita. Potencializada por IA.`

    **2. Hero CTA button:**
    `ENCONTRAR MINHA RAQUETE` → `Encontrar minha raquete`

    **3. CTA section button (second instance):**
    `ENCONTRAR MINHA RAQUETE` → `Encontrar minha raquete`

    **4. All section headings** — check for ALL CAPS in `<h2>` tags:
    - `COMO FUNCIONA` → `Como funciona`
    - Any other ALL CAPS headings → sentence case

    Note: With `font-display` (Bebas Neue), the text will still LOOK uppercase because Bebas Neue only has uppercase glyphs. But the actual text content should be sentence-case for accessibility (screen readers) and SEO.
  </action>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c 'Encontrar minha raquete' src/components/home/landing-client.tsx && grep -c 'ENCONTRAR MINHA RAQUETE' src/components/home/landing-client.tsx</automated>
  </verify>
  <done>
    - Zero ALL CAPS button text
    - Zero ALL CAPS headings (Bebas Neue renders them uppercase visually anyway)
    - Screen readers get proper sentence-case text
    - "Encontrar minha raquete" appears in both CTA buttons
  </done>
</task>

<task type="auto">
  <name>Task 5: Add trust signal section</name>
  <files>frontend/src/components/home/landing-client.tsx</files>
  <read_first>
    - frontend/src/components/home/landing-client.tsx (find insertion point between Features and CTA sections)
  </read_first>
  <action>
    Add a trust/social proof section between Features and CTA.

    Insert a new `<section>` with `bg-surface` between the Features section and the final CTA section. The section should contain:

    - Heading: "Por que jogadores confiam no PickleIQ"
    - 3 trust signal cards in a grid:
      1. **Dados reais** — preços coletados diariamente de varejistas brasileiros (Trophy icon)
      2. **IA personalizada** — recomendações baseadas no seu nível e estilo de jogo (Brain icon)
      3. **Comparação justa** — veja especificações e preços lado a lado (GitCompare icon)

    Use lucide-react icons (Trophy, Brain, GitCompare — check which are already imported).
    Wrap in AnimatedSection for scroll animation.
    All text in pt-BR.
    Use existing design tokens: `bg-brand-primary/10` for icon backgrounds, `text-brand-primary` for icons.

    This section satisfies the AI Slop Avoidance Checklist:
    - Cards exist for function (trust signals), not decoration
    - Content explains "why" (data freshness, personalization, fairness)
    - Labels are sentence case
  </action>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c 'Por que jogadores confiam' src/components/home/landing-client.tsx && grep -c 'Dados reais' src/components/home/landing-client.tsx</automated>
  </verify>
  <done>
    - Trust section exists between Features and CTA
    - Uses bg-surface for visual contrast
    - 3 trust signal cards with real content (data, AI, comparison)
    - Icons from lucide-react
    - Wrapped in AnimatedSection for scroll animation
    - pt-BR text throughout
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Landing client → /api/v1/paddles | Client-side fetch for paddle count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | D | fetchPaddles client-side | accept | API is public, no auth required — total count is not sensitive |
| T-30-07 | I | IntersectionObserver | accept | No security impact — purely visual animation |

</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript passes
2. `cd frontend && npm run build` — Build succeeds
3. Open homepage — visually verify ≥3 distinct section backgrounds
4. Scroll down — sections fade in as they enter viewport
5. Stats section shows real paddle count (check network tab for /paddles API call)
6. Verify buttons say "Encontrar minha raquete" (sentence case)
7. Open DevTools → enable `prefers-reduced-motion: reduce` → refresh → no animations, all sections visible
8. Check browser console — no IntersectionObserver memory leaks
9. Trust section visible between Features and CTA
</verification>

<success_criteria>
1. Landing page has ≥3 distinct section backgrounds (base, surface, elevated)
2. Scroll-triggered fade-in animations fire on all sections
3. Stats show real paddle count from API
4. All buttons use sentence case (zero ALL CAPS)
5. prefers-reduced-motion disables animations gracefully
6. IntersectionObserver cleaned up on unmount
7. Trust signal section present with real content
8. Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-conversion-landing-seo-footer-design/30-03-SUMMARY.md`
</output>
