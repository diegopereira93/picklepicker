---
phase: 02-full-data-pipeline
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/scrape.yml
  - pipeline/crawlers/mercado_livre.py
  - .env.example
autonomous: true
gap_closure: true
requirements: [R2.1]

must_haves:
  truths:
    - "GitHub Actions scrape-mercadolivre job imports pipeline.crawlers.mercado_livre successfully"
    - "python -m pipeline.crawlers.mercado_livre executes without ModuleNotFoundError"
    - ".env.example contains OPENAI_API_KEY and RAILWAY_API_TOKEN"
  artifacts:
    - path: ".github/workflows/scrape.yml"
      provides: "Correct module reference for Mercado Livre crawler"
      contains: "pipeline.crawlers.mercado_livre"
    - path: "pipeline/crawlers/mercado_livre.py"
      provides: "Entry point for python -m execution"
      contains: "if __name__"
    - path: ".env.example"
      provides: "All Phase 2 required environment variables"
      contains: "OPENAI_API_KEY"
  key_links:
    - from: ".github/workflows/scrape.yml"
      to: "pipeline/crawlers/mercado_livre.py"
      via: "python -m module import"
      pattern: "python -m pipeline\\.crawlers\\.mercado_livre"
---

<objective>
Fix crawler module naming mismatch that prevents GitHub Actions from executing the Mercado Livre crawler.

Purpose: GitHub Actions job `scrape-mercadolivre` references `pipeline.crawlers.mercadolivre_expansion` but the actual file is `pipeline/crawlers/mercado_livre.py`. This causes ModuleNotFoundError at runtime, blocking R2.1 (crawlers running via GH Actions schedule).

Output: Working GH Actions workflow that correctly imports and runs the Mercado Livre crawler module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-data-pipeline/02-VERIFICATION.md

Key interfaces from existing code:

From pipeline/crawlers/mercado_livre.py:
- `async def run_mercado_livre_crawler() -> int` — Main entry point, returns count of saved snapshots
- Uses `from pipeline.db.connection import get_connection`
- Uses `from pipeline.alerts.telegram import send_telegram_alert`

From pipeline/db/connection.py:
- `async def get_pool() -> AsyncConnectionPool`
- `async def get_connection()` — async context manager yielding psycopg connection
- `async def close_pool()`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GH Actions module reference and add __main__ block to crawler</name>
  <files>.github/workflows/scrape.yml, pipeline/crawlers/mercado_livre.py</files>
  <action>
1. In `.github/workflows/scrape.yml`, line 52: change `python -m pipeline.crawlers.mercadolivre_expansion` to `python -m pipeline.crawlers.mercado_livre`. This is the correct fix — update the reference to match the actual file, NOT rename the file. The file `mercado_livre.py` follows Python naming convention (snake_case with underscores matching the Portuguese "Mercado Livre").

2. In `pipeline/crawlers/mercado_livre.py`: add a `__main__` guard at the bottom of the file so `python -m` works:
```python
if __name__ == "__main__":
    import asyncio
    count = asyncio.run(run_mercado_livre_crawler())
    print(f"Saved {count} items from Mercado Livre")
```

3. Verify the dropshot_brasil.py also has a `__main__` guard (line 29 of scrape.yml runs `python -m pipeline.crawlers.dropshot_brasil`). If missing, add the same pattern calling `run_dropshot_brasil_crawler()`.
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/picklepicker && grep -q "pipeline.crawlers.mercado_livre" .github/workflows/scrape.yml && grep -q 'if __name__' pipeline/crawlers/mercado_livre.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>GH Actions scrape.yml references pipeline.crawlers.mercado_livre (not mercadolivre_expansion). Both crawler files have __main__ guard for python -m execution.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing Phase 2 env vars to .env.example</name>
  <files>.env.example</files>
  <action>
Add the two missing environment variables documented in VERIFICATION.md to `.env.example`:

1. `OPENAI_API_KEY=sk-<key>` — Required by pipeline/embeddings/batch_embedder.py for text-embedding-3-small
2. `RAILWAY_API_TOKEN=<token>` — Required for Railway deployment CLI

Add them under a new section comment `# Phase 2 — Embeddings & Deployment` after the existing Phase 2 crawler config section (after line 33 of current .env.example). Keep the existing structure and comment style.

Do NOT add env vars that are already present (GROQ_API_KEY, LANGFUSE_SECRET_KEY are already in Phase 3 section).
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/picklepicker && grep -q "OPENAI_API_KEY" .env.example && grep -q "RAILWAY_API_TOKEN" .env.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>OPENAI_API_KEY and RAILWAY_API_TOKEN present in .env.example with descriptive comments.</done>
</task>

</tasks>

<verification>
1. `grep "mercadolivre_expansion" .github/workflows/scrape.yml` returns NO matches (old reference removed)
2. `grep "pipeline.crawlers.mercado_livre" .github/workflows/scrape.yml` returns the correct import line
3. `python -c "import ast; ast.parse(open('pipeline/crawlers/mercado_livre.py').read()); print('VALID')"` confirms Python syntax
4. `grep -c "OPENAI_API_KEY\|RAILWAY_API_TOKEN" .env.example` returns 2
</verification>

<success_criteria>
- GitHub Actions workflow references `pipeline.crawlers.mercado_livre` (not `mercadolivre_expansion`)
- Both crawler files have `if __name__ == "__main__"` blocks for `python -m` execution
- .env.example contains OPENAI_API_KEY and RAILWAY_API_TOKEN entries
- No other workflow references to the old module name remain
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-data-pipeline/02-06-SUMMARY.md`
</output>
