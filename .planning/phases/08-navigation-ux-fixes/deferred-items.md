# Deferred Items — Phase 08

## Stale /compare References (out of scope for 08-01)

Discovered during overall verification after plan 08-01 tasks completed.

| File | Line | Issue |
|------|------|-------|
| frontend/src/components/layout/footer.tsx | (footer nav) | `href="/compare"` link — should point to /paddles |
| frontend/src/app/blog/pillar-page/page.tsx | (blog content) | `href="/compare"` link — should point to /paddles or be updated to reflect catalog |

These files were not in the 08-01 `files_modified` list and were not touched by any plan-01 task. They are pre-existing issues that should be fixed in a follow-up task or added to the 08 phase plan.
