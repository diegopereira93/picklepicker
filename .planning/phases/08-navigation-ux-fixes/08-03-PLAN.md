---
phase: 08-navigation-ux-fixes
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/app/paddles/page.tsx]
autonomous: true
requirements: []
gap_closure: true
---

<objective>
Fix catalog card link structure so Playwright test finds `<a>` tags inside `<article>` elements.

Purpose: The UAT test `page.locator('article').first().locator('a')` expects `<a>` as parent of `<article>`, but current structure has `<Link>` wrapping `<article>`. Need to restructure so `<article>` contains `<a href>`.

Output: Catalog cards with proper `<a>` structure that matches test expectations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-navigation-ux-fixes/08-HUMAN-UAT.md
@.planning/phases/08-navigation-ux-fixes/08-01-SUMMARY.md
@frontend/src/app/paddles/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Restructure catalog card with `<a>` inside `<article>`</name>
  <files>frontend/src/app/paddles/page.tsx</files>
  <behavior>
    - Test 1: `page.locator('article').locator('a')` finds links with valid hrefs
    - Test 2: Card href matches pattern `/paddles/{brand}/{model_slug_or_id}`
    - Test 3: Clicking card navigates to product page (not 404)
  </behavior>
  <action>
    Current structure (broken for test):
    ```jsx
    <Link href="/paddles/...">
      <article>...</article>
    </Link>
    ```

    Required structure (test-compatible):
    ```jsx
    <article>
      <a href="/paddles/...">
        {/* card content */}
      </a>
    </article>
    ```

    Changes:
    1. Replace Next.js `<Link>` with plain `<a>` tag inside `<article>`
    2. Keep all conditional rendering (skill_level badge, specs row, stock indicator)
    3. Preserve href pattern: `/paddles/${encodeURIComponent(paddle.brand?.toLowerCase() ?? '')}/${encodeURIComponent(paddle.model_slug ?? String(paddle.id))}`
    4. Remove `group` class from `<a>` (not needed without Link hover states)
    5. Keep `<article>` as semantic wrapper with border, padding, hover shadow

    Note: Using plain `<a>` instead of `<Link>` is acceptable here because:
    - Catalog is SSR static content (ISR revalidate 3600s)
    - No client-side navigation optimization needed for SEO pages
    - Test requires standard `<a>` selector
  </action>
  <verify>
    <automated>grep -q '<a href=' frontend/src/app/paddles/page.tsx && echo "PASS: <a> tag found"</automated>
  </verify>
  <done>Card structure changed from `<Link><article>` to `<article><a>` with href attribute</done>
</task>

</tasks>

<verification>
Run Playwright test to verify card link structure:
```bash
cd frontend && npx playwright test 08-navigation-ux-fixes.playwright.ts --grep "Catalog card numeric-ID fallback"
```
Expected: Test passes, card href found, product page loads without 404.
</verification>

<success_criteria>
- [ ] `<a href>` element exists inside `<article>` in paddles/page.tsx
- [ ] href attribute matches `/paddles/{brand}/{slug_or_id}` pattern
- [ ] Playwright test 1 passes (no 404, product info visible)
- [ ] Visual card layout unchanged (skill_level badge, specs, stock indicator still render)
</success_criteria>

<output>
After completion, create `.planning/phases/08-navigation-ux-fixes/08-03-SUMMARY.md`
</output>
