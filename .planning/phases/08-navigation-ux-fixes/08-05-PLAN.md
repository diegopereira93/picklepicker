---
phase: 08-navigation-ux-fixes
plan: "05"
type: execute
wave: 1
depends_on: [08-01]
files_modified: [frontend/src/components/layout/header.tsx]
autonomous: true
requirements: [NAV-02]
gap_closure: true
---

<objective>
Remove "Encontrar raquete" CTA button from mobile hamburger menu to match desktop nav structure.

Purpose: UAT Test 2 failed because mobile nav shows Chat IA link. The desktop navLinks array is correct (Home, Catalogo only), but the mobile Sheet has a hardcoded "Encontrar raquete" button linking to /chat that must be removed.

Output: Mobile hamburger menu shows only Home, Catalogo links (consistent with desktop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-navigation-ux-fixes/08-HUMAN-UAT.md
@.planning/phases/08-navigation-ux-fixes/08-01-SUMMARY.md
@frontend/src/components/layout/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove "Encontrar raquete" button from mobile Sheet</name>
  <files>frontend/src/components/layout/header.tsx</files>
  <read_first>
    - frontend/src/components/layout/header.tsx
  </read_first>
  <action>
    In `frontend/src/components/layout/header.tsx`, find the SheetContent component for mobile navigation (around lines 69-104).

    Locate and REMOVE the following block (lines 88-92):

    ```tsx
    <Button asChild className="mt-4">
      <Link href="/chat" onClick={() => setOpen(false)}>
        Encontrar raquete
      </Link>
    </Button>
    ```

    This button is inside SheetContent, separate from the navLinks mapping. It creates a "Chat IA" entry in the mobile menu that doesn't exist on desktop.

    Keep:
    - The navLinks.map() rendering (lines 78-87) - already correct with [Home, Catalogo]
    - Auth buttons (Login/Sign Up) in mobile menu
    - Desktop "Encontrar raquete" CTA button (line 50) - this should stay

    Only remove the mobile-specific "Encontrar raquete" button block.
  </action>
  <verify>
    <automated>cd /home/diego/Documentos/picklepicker && grep -c "Encontrar raquete" frontend/src/components/layout/header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Encontrar raquete' frontend/src/components/layout/header.tsx` returns exactly 1 match (desktop CTA only)
    - Mobile Sheet content has no Button linking to /chat
    - navLinks.map() rendering unchanged (Home, Catalogo only)
  </acceptance_criteria>
  <done>Mobile hamburger menu shows only Home, Catalogo links - no "Encontrar raquete" or Chat IA entry. Desktop nav unchanged.</done>
</task>

</tasks>

<verification>
After fix, run Playwright test:
```bash
cd frontend && npx playwright test 08-navigation-ux-fixes.playwright.ts --grep "Header nav structure"
```
Expected: Test passes - desktop and mobile both show only Home, Catalogo (no Chat IA).
</verification>

<success_criteria>
- [ ] Mobile Sheet has no "Encontrar raquete" button
- [ ] Mobile navLinks map renders only Home, Catalogo
- [ ] Desktop "Encontrar raquete" CTA preserved (line 50)
- [ ] Playwright Test 2 passes (mobileHasChatIA = false)
- [ ] Nav consistency: desktop and mobile show same links
</success_criteria>

<output>
After completion, create `.planning/phases/08-navigation-ux-fixes/08-05-SUMMARY.md`
</output>
