---
phase: 24-fix-pipeline-tests-recriar-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/tests/fixtures/mock_responses/brazil_store_response.json
  - pipeline/tests/fixtures/mock_responses/dropshot_brasil_response.json
  - pipeline/tests/fixtures/mock_responses/mercado_livre_response.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "load_mock_response('brazil_store_response.json') returns dict with data.products array"
    - "load_mock_response('dropshot_brasil_response.json') returns dict with data.products array"
    - "load_mock_response('mercado_livre_response.json') returns dict with results array"
    - "Each Brazil/Dropshot product has: name, price_brl (positive number), in_stock (bool), image_url (https), product_url (https), brand (non-empty string), specs (dict)"
    - "Each ML item has: id, title, price, permalink"
    - "All products have unique names within their scraper"
    - "ML items have unique ids"
  artifacts:
    - path: "pipeline/tests/fixtures/mock_responses/brazil_store_response.json"
      provides: "Brazil Store mock product data for all Brazil Store tests"
      contains: "data"
    - path: "pipeline/tests/fixtures/mock_responses/dropshot_brasil_response.json"
      provides: "Dropshot Brasil mock product data for all Dropshot tests"
      contains: "data"
    - path: "pipeline/tests/fixtures/mock_responses/mercado_livre_response.json"
      provides: "Mercado Livre mock search results for all ML tests"
      contains: "results"
  key_links:
    - from: "test_data_integrity.py"
      to: "brazil_store_response.json"
      via: "load_mock_response at module level line 40"
      pattern: "load_mock_response.*brazil_store_response"
    - from: "test_data_integrity.py"
      to: "dropshot_brasil_response.json"
      via: "load_mock_response at module level line 41"
      pattern: "load_mock_response.*dropshot_brasil_response"
    - from: "test_data_integrity.py"
      to: "mercado_livre_response.json"
      via: "load_mock_response at module level line 42"
      pattern: "load_mock_response.*mercado_livre_response"
---

<objective>
Create all 3 missing mock response fixture JSON files to unblock 130 pipeline tests.

Purpose: The pipeline test suite is completely broken because `test_data_integrity.py` loads fixtures at module-level (lines 40-42). Missing fixtures cause FileNotFoundError during test collection, preventing ANY pipeline test from running. Recreating valid fixtures restores full test visibility.

Output: 3 JSON fixture files in `pipeline/tests/fixtures/mock_responses/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pipeline/tests/test_utils.py
@pipeline/tests/test_data_integrity.py
@pipeline/tests/conftest.py
@pipeline/crawlers/brazil_store.py
@pipeline/crawlers/dropshot_brasil.py
@pipeline/crawlers/mercado_livre.py
</context>

<interfaces>
<!-- PRODUCT_SCHEMA from test_utils.py — every product in fixtures MUST match -->
```python
PRODUCT_SCHEMA = {
    "name": str,
    "price_brl": (int, float),
    "in_stock": bool,
    "image_url": str,
    "product_url": str,
    "brand": str,
    "specs": dict,
}
```

<!-- How test files access the fixtures -->
```python
# test_data_integrity.py (module-level — BLOCKS ALL TESTS if FileNotFoundError)
BRAZIL_PRODUCTS = load_mock_response("brazil_store_response.json")["data"]["products"]
DROPSHOT_PRODUCTS = load_mock_response("dropshot_brasil_response.json")["data"]["products"]
ML_ITEMS = load_mock_response("mercado_livre_response.json")["results"]

# test_brazil_store_scraper.py
data = load_mock_response("brazil_store_response.json")
products = data["data"]["products"]

# test_dropshot_brasil_scraper.py
data = load_mock_response("dropshot_brasil_response.json")
products = data["data"]["products"]

# test_mercado_livre_scraper.py
data = load_mock_response("mercado_livre_response.json")
products = data["results"]

# test_mercadolivre_expansion.py
data = load_mock_response("mercado_livre_response.json")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Brazil Store mock response fixture</name>
  <files>pipeline/tests/fixtures/mock_responses/brazil_store_response.json</files>
  <behavior>
    - Test 1: python -c "import json; d=json.load(open('pipeline/tests/fixtures/mock_responses/brazil_store_response.json')); assert isinstance(d['data']['products'], list); assert len(d['data']['products']) >= 3"
    - Test 2: python -c "import json; d=json.load(open('pipeline/tests/fixtures/mock_responses/brazil_store_response.json')); [assert p['price_brl'] > 0 and isinstance(p['price_brl'], (int,float)) and 'brazilpickleballstore' in p['product_url'] for p in d['data']['products']]" (conceptual — validate each product)
    - Test 3: All products must pass assert_schema_valid() from test_utils.py
    - Test 4: All product names must be unique within the fixture
  </behavior>
  <read_first>
    - pipeline/tests/test_utils.py (PRODUCT_SCHEMA and load_mock_response function)
    - pipeline/tests/test_data_integrity.py (lines 40, 54-111 — how data is consumed)
    - pipeline/tests/test_brazil_store_scraper.py (fixture at line 38, all tests consuming it)
    - pipeline/crawlers/brazil_store.py (FIRECRAWL_SCHEMA at line 21-34, extract_products output format at line 87-98)
  </read_first>
  <action>Create directory `pipeline/tests/fixtures/mock_responses/` (mkdir -p).

Then create `pipeline/tests/fixtures/mock_responses/brazil_store_response.json` with a JSON object following this EXACT structure:

```json
{
  "data": {
    "products": [
      {
        "name": "Selkirk Vanguard Power Air",
        "price_brl": 1299.90,
        "in_stock": true,
        "image_url": "https://brazilpickleballstore.com.br/wp-content/uploads/selkirk-vanguard.jpg",
        "product_url": "https://brazilpickleballstore.com.br/produto/selkirk-vanguard-power-air/",
        "brand": "Selkirk",
        "specs": {
          "peso": "226g",
          "espessura": "13mm",
          "material": "T700 Carbon Fiber"
        }
      },
      ... (at least 3 more products with unique names)
    ]
  }
}
```

Requirements:
1. MUST have exactly the shape `{"data": {"products": [...]}}` — this is how Firecrawl wraps extraction results
2. Include at least 4 products with unique names (Selkirk, JOOLA, Engage, Diadem — real pickleball brands)
3. Every product MUST have all 7 fields from PRODUCT_SCHEMA: name (str), price_brl (positive number), in_stock (bool), image_url (str starting with https://), product_url (str starting with https://, containing "brazilpickleballstore"), brand (non-empty str), specs (dict, can be empty {})
4. Use realistic Brazilian Reais prices (R$400-R$2000 range)
5. product_url must contain "brazilpickleballstore" domain (tests assert this at test_brazil_store_scraper.py line 93)
6. image_url must start with "https://" (tests assert at test_data_integrity.py line 85)
7. brand must be non-empty string (tests assert at test_data_integrity.py line 74-79)
8. Each product name must be unique (tests assert at test_data_integrity.py line 289-294)
</action>
  <acceptance_criteria>
    - `pipeline/tests/fixtures/mock_responses/brazil_store_response.json` exists
    - `python3 -c "import json; d=json.load(open('pipeline/tests/fixtures/mock_responses/brazil_store_response.json')); assert 'data' in d and 'products' in d['data']"` exits 0
    - `python3 -c "import json; d=json.load(open('pipeline/tests/fixtures/mock_responses/brazil_store_response.json')); ps=d['data']['products']; assert len(ps) >= 3"` exits 0
    - Every product has keys: name, price_brl, in_stock, image_url, product_url, brand, specs
    - Every product_url contains "brazilpickleballstore"
    - Every image_url starts with "https://"
    - All price_brl values are positive numbers
    - All brand values are non-empty strings
    - All product names are unique
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Documentos/picklepicker && python3 -c "
import json
d = json.load(open('pipeline/tests/fixtures/mock_responses/brazil_store_response.json'))
ps = d['data']['products']
assert len(ps) >= 3, f'Need >= 3 products, got {len(ps)}'
required = ['name','price_brl','in_stock','image_url','product_url','brand','specs']
for p in ps:
    for f in required:
        assert f in p, f'Missing {f} in {p.get(\"name\",\"?\")}'
    assert isinstance(p['price_brl'], (int,float)) and p['price_brl'] > 0
    assert isinstance(p['in_stock'], bool)
    assert p['image_url'].startswith('https://')
    assert 'brazilpickleballstore' in p['product_url']
    assert len(p['brand'].strip()) > 0
    assert isinstance(p['specs'], dict)
names = [p['name'] for p in ps]
assert len(names) == len(set(names)), 'Duplicate product names'
print(f'PASS: {len(ps)} Brazil Store products validated')
"</automated>
  </verify>
  <done>brazil_store_response.json exists with >= 3 valid products matching PRODUCT_SCHEMA, all tests can load it without FileNotFoundError</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Dropshot Brasil and Mercado Livre mock response fixtures</name>
  <files>pipeline/tests/fixtures/mock_responses/dropshot_brasil_response.json, pipeline/tests/fixtures/mock_responses/mercado_livre_response.json</files>
  <behavior>
    - Test 1: dropshot_brasil_response.json loads with data.products array, all products have required fields, product_url contains "dropshotbrasil"
    - Test 2: mercado_livre_response.json loads with results array, each item has id, title, price, permalink, all unique ids
    - Test 3: All Dropshot products pass assert_schema_valid()
    - Test 4: ML items pass test_ml_items_have_required_fields (id, title, price, permalink all non-None)
    - Test 5: build_affiliate_url(item["permalink"], "TESTMATT") contains "matt_id=TESTMATT" for each ML item
  </behavior>
  <read_first>
    - pipeline/tests/test_utils.py (PRODUCT_SCHEMA, load_mock_response)
    - pipeline/tests/test_data_integrity.py (lines 40-42, 97-103, 153-160)
    - pipeline/tests/test_dropshot_brasil_scraper.py (fixture at line 36-39, all test methods)
    - pipeline/tests/test_mercado_livre_scraper.py (fixtures at lines 31-41, all test methods)
    - pipeline/crawlers/dropshot_brasil.py (FIRECRAWL_SCHEMA, extract_products output)
    - pipeline/crawlers/mercado_livre.py (build_affiliate_url, search_pickleball_paddles, save_ml_items_to_db)
  </read_first>
  <action>Create two fixture files:

**File 1: `pipeline/tests/fixtures/mock_responses/dropshot_brasil_response.json`**

Structure: `{"data": {"products": [...]}}` (same Firecrawl wrapper as Brazil Store)

Include at least 2 products (test_dropshot_brasil_scraper.py line 246 asserts `result == 2`). Requirements:
- All 7 PRODUCT_SCHEMA fields per product
- product_url must contain "dropshotbrasil" (test line 97 asserts this)
- image_url must start with "https://"
- brand must be non-empty
- price_brl must be positive number
- Specs can include: espessura, material, peso, formato
- Use Drop Shot brand and other real pickleball brands
- Product names must be unique

Example product:
```json
{
  "name": "Drop Shot Explorer 3.0",
  "price_brl": 459.90,
  "in_stock": true,
  "image_url": "https://dropshotbrasil.com.br/wp-content/uploads/explorer-3.jpg",
  "product_url": "https://www.dropshotbrasil.com.br/raquetes/drop-shot-explorer-3-0",
  "brand": "Drop Shot",
  "specs": {"espessura": "13mm", "material": "Carbon Fiber"}
}
```

**File 2: `pipeline/tests/fixtures/mock_responses/mercado_livre_response.json`**

Structure mimics Mercado Livre search API response:
```json
{
  "site_id": "MLB",
  "query": "raquete pickleball",
  "results": [
    {
      "id": "MLB123456789",
      "title": "Raquete Pickleball Selkirk Vanguard Power Air",
      "price": 1299.90,
      "currency_id": "BRL",
      "available_quantity": 5,
      "permalink": "https://www.mercadolivre.com.br/raquete-pickleball-selkirk-vanguard-MLB123456789",
      "thumbnail": "http://http2.mlstatic.com/D_123456-MLB.jpg",
      "official_image_url": "https://http2.mlstatic.com/D_123456-MLB.jpg",
      "installments": {"quantity": 10, "amount": 129.99, "rate": 0, "currency_id": "BRL"},
      "shipping": {"free_shipping": true}
    },
    ... (at least 3 more items)
  ],
  "paging": {"total": 50, "offset": 0, "limit": 50}
}
```

Requirements:
- MUST have `results` key with array (test_data_integrity.py line 42: `load_mock_response("mercado_livre_response.json")["results"]`)
- Each item MUST have: id (unique string starting with "MLB"), title (str), price (positive number), permalink (str starting with https://www.mercadolivre.com.br/)
- price must NOT be 0 (save_ml_items_to_db skips zero-price items — test line 300)
- All ids must be unique (test_data_integrity.py line 296)
- permalink must be a valid URL (build_affiliate_url uses it — test line 153-160)
- Include at least 4 items to exercise pagination/dedup tests
- Use real-sounding pickleball product titles in Portuguese
</action>
  <acceptance_criteria>
    - `pipeline/tests/fixtures/mock_responses/dropshot_brasil_response.json` exists
    - `pipeline/tests/fixtures/mock_responses/mercado_livre_response.json` exists
    - Dropshot: data.products array with >= 2 products, all matching PRODUCT_SCHEMA, all product_url contain "dropshotbrasil"
    - ML: results array with >= 4 items, each with id/title/price/permalink, all ids unique, all prices > 0
    - Dropshot brand field non-empty for all products
    - ML permalink URLs start with "https://www.mercadolivre.com.br/"
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Documentos/picklepicker && python3 -c "
import json

# Validate Dropshot
d = json.load(open('pipeline/tests/fixtures/mock_responses/dropshot_brasil_response.json'))
ps = d['data']['products']
assert len(ps) >= 2, f'Dropshot: need >= 2 products, got {len(ps)}'
required = ['name','price_brl','in_stock','image_url','product_url','brand','specs']
for p in ps:
    for f in required:
        assert f in p, f'Dropshot missing {f}'
    assert p['price_brl'] > 0
    assert isinstance(p['in_stock'], bool)
    assert p['image_url'].startswith('https://')
    assert 'dropshotbrasil' in p['product_url'], f'Expected dropshotbrasil in {p[\"product_url\"]}'
    assert len(p['brand'].strip()) > 0
    assert isinstance(p['specs'], dict)
names = [p['name'] for p in ps]
assert len(names) == len(set(names)), 'Dropshot duplicate names'
print(f'Dropshot PASS: {len(ps)} products')

# Validate ML
ml = json.load(open('pipeline/tests/fixtures/mock_responses/mercado_livre_response.json'))
items = ml['results']
assert len(items) >= 4, f'ML: need >= 4 items, got {len(items)}'
ml_required = ['id', 'title', 'price', 'permalink']
for item in items:
    for f in ml_required:
        assert f in item, f'ML missing {f}'
        assert item[f] is not None, f'ML {f} is None'
    assert item['price'] > 0, f'ML price must be >0, got {item[\"price\"]}'
    assert item['permalink'].startswith('https://www.mercadolivre.com.br/'), f'Bad permalink: {item[\"permalink\"]}'
ids = [i['id'] for i in items]
assert len(ids) == len(set(ids)), 'ML duplicate ids'
print(f'ML PASS: {len(items)} items')
print('ALL FIXTURES VALID')
"</automated>
  </verify>
  <done>Both fixture files exist and pass all validation checks. test_data_integrity.py module-level imports no longer raise FileNotFoundError.</done>
</task>

</tasks>

<verification>
1. All 3 JSON files exist in `pipeline/tests/fixtures/mock_responses/`
2. Each file is valid JSON parseable by `json.load()`
3. Brazil Store and Dropshot have `data.products` array with valid PRODUCT_SCHEMA entries
4. Mercado Livre has `results` array with id, title, price, permalink
5. No duplicate names within same scraper, no duplicate ML ids
6. All URLs use https://, all prices positive, all brands non-empty
</verification>

<success_criteria>
1. `python3 -c "import json; [json.load(open(f'pipeline/tests/fixtures/mock_responses/{f}')) for f in ['brazil_store_response.json', 'dropshot_brasil_response.json', 'mercado_livre_response.json']]"` exits 0
2. All products/items pass their respective schema validations
3. No FileNotFoundError when test_utils.load_mock_response() is called
</success_criteria>

<output>
After completion, create `.planning/phases/24-fix-pipeline-tests-recriar-fixtures/24-01-SUMMARY.md`
</output>
