---
phase: 31-polish-compare-3-4-breadcrumbs-error-boundaries
plan: "02"
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - frontend/src/components/ui/breadcrumb.tsx
  - frontend/src/app/catalog/[slug]/page.tsx
  - frontend/src/app/compare/page.tsx
autonomous: true
requirements:
  - P31-TASK-31.2
  - P31-TASK-31.4
user_setup: []

must_haves:
  truths:
    - Product detail page shows breadcrumb navigation (Início > Catálogo > {Paddle})
    - Compare page shows breadcrumb navigation (Início > Comparar)
    - BreadcrumbList JSON-LD included in both pages
    - Specs table has alternating row backgrounds (base/elevated)
  artifacts:
    - path: frontend/src/components/ui/breadcrumb.tsx
      provides: "Reusable breadcrumb component"
      contains: "BreadcrumbProps"
    - path: frontend/src/app/catalog/[slug]/page.tsx
      provides: "Detail page with breadcrumbs"
      contains: "breadcrumb"
  key_links:
    - from: frontend/src/components/ui/breadcrumb.tsx
      to: frontend/src/app/catalog/[slug]/page.tsx
      via: "import and render"
      pattern: "import.*breadcrumb.*from"
---

<objective>
Add breadcrumb navigation to product detail and compare pages with JSON-LD structured data. Add alternating row backgrounds to specs table.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-polish-compare-3-4-breadcrumbs-error-boundaries/31-RESEARCH.md
@frontend/src/app/catalog/[slug]/page.tsx
@frontend/src/app/compare/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Breadcrumb component</name>
  <files>frontend/src/components/ui/breadcrumb.tsx</files>
  <read_first>
    - frontend/src/app/globals.css (existing design tokens: text-text-secondary, text-text-muted)
    - DESIGN.md (existing icon patterns, separator style)
  </read_first>
  <action>
    Create new component at `frontend/src/components/ui/breadcrumb.tsx`:
    ```typescript
    interface BreadcrumbItem {
      label: string
      href?: string
    }

    interface BreadcrumbProps {
      items: BreadcrumbItem[]
      className?: string
    }
    ```
    
    - Render items with chevron separator between them
    - First item: Home icon (lucide-react) if href present, otherwise just icon
    - Last item: bold text, no link
    - Intermediate items: muted text, links
    - Wrapper: flex, items-center, gap-2, text-sm
    - Add JSON-LD script tag with BreadcrumbList schema (for SEO)
  </action>
  <acceptance_criteria>
    - File exists: frontend/src/components/ui/breadcrumb.tsx
    - Interface has `items:` property with BreadcrumbItem[]
    - Returns both visual breadcrumb and JSON-LD script
  </acceptance_criteria>
  <verify>
    <automated>ls -la frontend/src/components/ui/breadcrumb.tsx 2>&1</automated>
  </verify>
  <done>Breadcrumb component created with JSON-LD</done>
</task>

<task type="auto">
  <name>Task 2: Add breadcrumbs to product detail page</name>
  <files>frontend/src/app/catalog/[slug]/page.tsx</files>
  <read_first>
    - frontend/src/app/catalog/[slug]/page.tsx (line 60+ section for specs table)
    - frontend/src/components/ui/breadcrumb.tsx (after Task 1)
  </read_first>
  <action>
    Import Breadcrumb and add to product detail page (after Header or as first content):
    ```typescript
    import { Breadcrumb } from '@/components/ui/breadcrumb'
    
    // In render:
    <Breadcrumb
      items={[
        { label: 'Início', href: '/' },
        { label: 'Catálogo', href: '/catalog' },
        { label: `${paddle.brand} ${paddle.name}`, href: undefined }
      ]}
    />
    ```
    
    - Place above product name/hero section
    - Use paddle.brand + paddle.name for third item (current page)
  </action>
  <acceptance_criteria>
    - grep "Breadcrumb" frontend/src/app/catalog/[slug]/page.tsx returns matches
    - Page renders breadcrumb with 3 items
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Breadcrumb visible on product detail page</done>
</task>

<task type="auto">
  <name>Task 3: Add breadcrumbs to compare page</name>
  <files>frontend/src/app/compare/page.tsx</files>
  <read_first>
    - frontend/src/app/compare/page.tsx (line 1+ for structure)
    - frontend/src/components/ui/breadcrumb.tsx (after Task 1)
  </read_first>
  <action>
    Import Breadcrumb and add to compare page:
    ```typescript
    import { Breadcrumb } from '@/components/ui/breadcrumb'
    
    // In render, after suspension fallback:
    <Breadcrumb
      items={[
        { label: 'Início', href: '/' },
        { label: 'Comparar Raquetes', href: undefined }
      ]}
    />
    ```
  </action>
  <acceptance_criteria>
    - grep "Breadcrumb" frontend/src/app/compare/page.tsx returns matches
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Breadcrumb visible on compare page</done>
</task>

<task type="auto">
  <name>Task 4: Add alternating row backgrounds to specs table</name>
  <files>frontend/src/app/catalog/[slug]/page.tsx</files>
  <read_first>
    - frontend/src/app/catalog/[slug]/page.tsx (search for "grid-cols-2 gap-4")
  </read_first>
  <action>
    Find the specs table section (around line 200-250) with grid-cols-2 layout.
    Replace static class with index-based alternating:
    ```typescript
    // Current:
    <div className={cn('grid grid-cols-2 gap-4', 'px-4 py-3')}>
    
    // Updated:
    <div className={cn(
      'grid grid-cols-2 gap-4 px-4 py-3',
      index % 2 === 0 ? 'bg-surface' : 'bg-elevated'
    )}>
    ```
    
    - Apply to EACH spec row div (inside the .map() or .entries() iteration)
    - Ensure first row is bg-surface, second is bg-elevated, repeat
  </action>
  <acceptance_criteria>
    - grep "index % 2" frontend/src/app/catalog/[slug]/page.tsx returns matches
    - Rows alternate between bg-surface and bg-elevated
  </acceptance_criteria>
  <verify>
    <automated>grep "bg-surface\|bg-elevated" frontend/src/app/catalog/\\[slug\\]/page.tsx | wc -l</automated>
  </verify>
  <done>Specs table rows alternate background colors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR→client | JSON-LD for SEO, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Spoofing | Breadcrumb links | accept | Links are internal, relative |
| T-31-04 | Information Disclosure | JSON-LD | accept | Public product data for SEO |
</threat_model>

<verification>
- Navigate to catalog/selkirk-ambit-xl and see breadcrumb (Início > Catálogo > Selkirk Ambit XL)
- Navigate to /compare?a=1&b=2 and see breadcrumb (Início > Comparar Raquetes)
- Inspect page source and find BreadcrumbList JSON-LD script
- Specs table rows alternate base/elevated background
</verification>

<success_criteria>
1. Breadcrumbs visible on product detail and compare pages
2. JSON-LD BreadcrumbList in page source
3. Specs table has alternating row backgrounds
4. No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-polish-compare-3-4-breadcrumbs-error-boundaries/31-02-SUMMARY.md`
</output>