---
phase: 31-polish-compare-3-4-breadcrumbs-error-boundaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/compare-row.tsx
  - frontend/src/app/compare/page.tsx
autonomous: true
requirements:
  - P31-TASK-31.1
user_setup: []

must_haves:
  truths:
    - Compare page renders 2-4 paddles from URL params (?a=1&b=2&c=3&d=4)
    - CompareRow component accepts N values (not just valueA/valueB)
    - Responsive grid: 2 cols at md, 3 cols at lg, 4 cols at xl
    - Invalid paddle count (0,1,5+) shows user-friendly message
  artifacts:
    - path: frontend/src/components/ui/compare-row.tsx
      provides: "N-column comparison row"
      contains: "values: (string|number|null)[]"
    - path: frontend/src/app/compare/page.tsx
      provides: "3-4 paddle comparison"
      contains: "paddleIds.map"
  key_links:
    - from: frontend/src/app/compare/page.tsx
      to: frontend/src/components/ui/compare-row.tsx
      via: "props.values array"
      pattern: "values.*\\["
---

<objective>
Refactor compare page to support 2-4 paddles (currently only 2). Extend CompareRow component to accept N values instead of hardcoded valueA/valueB.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-polish-compare-3-4-breadcrumbs-error-boundaries/31-RESEARCH.md
@frontend/src/components/ui/compare-row.tsx
@frontend/src/app/compare/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CompareRow for N columns</name>
  <files>frontend/src/components/ui/compare-row.tsx</files>
  <read_first>
    - frontend/src/components/ui/compare-row.tsx (current implementation with valueA/valueB)
    - frontend/src/app/globals.css (existing design tokens: bg-surface, bg-elevated)
  </read_first>
  <action>
    Refactor interface from:
    ```typescript
    interface CompareRowProps {
      attribute: string
      valueA: string | number | null | undefined
      valueB: string | number | null | undefined
      winner?: 'a' | 'b' | 'tie'
      className?: string
    }
    ```
    To:
    ```typescript
    interface CompareRowProps {
      attribute: string
      values: (string | number | null)[]
      winners?: ('a' | 'b' | 'c' | 'd' | 'tie' | null)[]
      className?: string
    }
    ```
    
    - Update rendering to map over `values` array: `values.map((val, idx) => ...)`
    - For each value column: add data-index attribute for styling (0=a, 1=b, 2=c, 3=d)
    - Keep Check icon for winner matching column index
    - Handle null/undefined: display "—"
    - Format numbers with toFixed(1), strings as-is
  </action>
  <acceptance_criteria>
    - grep "values\[" frontend/src/components/ui/compare-row.tsx returns matches
    - Interface has `values:` property (not valueA/valueB)
    - Each column has data-index attribute (data-index="0", data-index="1", etc.)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit frontend/src/components/ui/compare-row.tsx 2>&1 | head -20</automated>
  </verify>
  <done>CompareRow accepts values array and renders N columns</done>
</task>

<task type="auto">
  <name>Task 2: Update compare page for 3-4 paddles</name>
  <files>frontend/src/app/compare/page.tsx</files>
  <read_first>
    - frontend/src/app/compare/page.tsx (current ?a=ID&b=ID handling)
    - frontend/src/components/ui/compare-row.tsx (after Task 1 refactor)
  </read_first>
  <action>
    Replace 2-paddle URL param handling:
    ```typescript
    const a = searchParams.get('a')
    const b = searchParams.get('b')
    ```
    With N-paddle approach:
    ```typescript
    const paddleIdKeys = ['a', 'b', 'c', 'd']
    const paddleIds = paddleIdKeys
      .map(key => searchParams.get(key))
      .filter(Boolean)
      .map(Number)
      .filter(id => id > 0)
    ```
    
    - Validate: reject if paddleIds.length < 2 or > 4 with error message in Portuguese
    - Fetch all paddles: `await Promise.all(paddleIds.map(id => fetchPaddle(id)))`
    - Update CompareRow rendering to pass `values` prop with N entries
    - Update grid to responsive: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`
    - Hide RadarChart when > 2 paddles (not compatible)
    - Hide CompareVerdict when > 2 paddles (only for 2)
    - Update buy CTA grid: dynamic columns based on paddle count
  </action>
  <acceptance_criteria>
    - grep "paddleIds" frontend/src/app/compare/page.tsx returns matches
    - grep "grid-cols-" frontend/src/app/compare/page.tsx contains "grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"
    - Page shows "Selecione 2-4 raquetes" error when count < 2 or > 4
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Compare page loads 2-4 paddles from URL params</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→compare page | URL params are user input, validate strictly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | URL params (?a=1&b=2&c=3&d=4) | mitigate | Validate: reject non-numeric, < 2, > 4, non-existent IDs |
| T-31-02 | DoS | Large Promise.all | mitigate | Limit to max 4 fetches, timeout each |
</threat_model>

<verification>
- Compare page renders with ?a=1&b=2 (2 paddles, current)
- Compare page renders with ?a=1&b=2&c=3 (3 paddles, new)
- Compare page renders with ?a=1&b=2&c=3&d=4 (4 paddles, new)
- Compare page shows error with ?a=1 (1 paddle)
- Compare page shows error with ?a=1&b=2&c=3&d=4&e=5 (5 paddles)
</verification>

<success_criteria>
1. Compare page supports 2-4 paddles via URL params
2. CompareRow renders N columns dynamically
3. Responsive grid works at all breakpoints
4. Error message for invalid paddle count
5. No TypeScript errors after refactor
</success_criteria>

<output>
After completion, create `.planning/phases/31-polish-compare-3-4-breadcrumbs-error-boundaries/31-01-SUMMARY.md`
</output>