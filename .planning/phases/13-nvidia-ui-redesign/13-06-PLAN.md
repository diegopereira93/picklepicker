---
phase: 13-nvidia-ui-redesign
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/button.tsx
autonomous: true
gap_closure: true
requirements:
  - HY-04
must_haves:
  truths:
    - "Primary buttons use lime (#84CC16) border per Hybrid spec"
    - "Button border color uses sport-primary, not data-green"
  artifacts:
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Button variants with correct border colors"
      contains: "#84CC16"
  key_links:
    - from: "button.tsx"
      to: "globals.css"
      via: "border color hex values"
      pattern: "#84CC16|var\\(--sport-primary\\)"
---

<objective>
Fix button border color to use lime (#84CC16) instead of data-green (#76b900).

Purpose: The Hybrid design spec defines lime (#84CC16) as the sport-primary color for primary action borders on dark backgrounds. Currently button.tsx uses #76b900 which is the data-green color reserved for charts, tables, and section labels.
Output: button.tsx with corrected border colors matching HY-04 specification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-nvidia-ui-redesign/CONTEXT.md

<interfaces>
<!-- From CONTEXT.md HY-04 specification -->
**Primary (green-border on transparent):**
```css
background: transparent;
border: 2px solid #84CC16;
color: #ffffff;
```

<!-- From globals.css -->
```css
--sport-primary: #84CC16;        /* lime-500: primary actions on dark */
--data-green: #76b900;           /* green for charts, tables, section labels */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace #76b900 with #84CC16 in button variants</name>
  <files>frontend/src/components/ui/button.tsx</files>
  <read_first>
    - frontend/src/components/ui/button.tsx (current state with #76b900 borders)
    - .planning/phases/13-nvidia-ui-redesign/CONTEXT.md (HY-04 specification)
  </read_first>
  <behavior>
    - All button variants use #84CC16 (lime) for borders, not #76b900 (data-green)
    - Link variant can keep #76b900 as text color (it's a link, not a border)
    - Hover/active states remain unchanged
  </behavior>
  <action>
    Modify frontend/src/components/ui/button.tsx:

    Replace ALL occurrences of `#76b900` with `#84CC16` in button variant definitions, EXCEPT:
    - The `link` variant's text color should remain `#76b900` (it's not a border)
    - The `light` variant's hover background should remain `#76b900` (data-green on light is acceptable)

    Specifically update these variants:
    - `default`: Change `border-[#76b900]` to `border-[#84CC16]`
    - `secondary`: Change `border-[#76b900]` to `border-[#84CC16]`
    - `light`: Change `border-[#76b900]` to `border-[#84CC16]` (but keep hover:bg-[#76b900])
    - `compact`: Change `border-[#76b900]` to `border-[#84CC16]`
    - `outline`: Change `border-[#76b900]` to `border-[#84CC16]`

    Do NOT change:
    - The `link` variant text color (keep `text-[#76b900]`)
    - The `light` variant hover background (keep `hover:bg-[#76b900]`)
    - The `light` variant active background (keep `active:bg-[#5a9100]`)
  </action>
  <acceptance_criteria>
    - grep 'border-\\[#84CC16\\]' frontend/src/components/ui/button.tsx returns at least 5 matches (one per variant)
    - grep 'border-\\[#76b900\\]' frontend/src/components/ui/button.tsx returns ZERO matches
    - grep 'text-\\[#76b900\\]' frontend/src/components/ui/button.tsx returns 1 match (link variant)
    - grep 'hover:bg-\\[#76b900\\]' frontend/src/components/ui/button.tsx returns 1 match (light variant)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'border-\\[#84CC16\\]' frontend/src/components/ui/button.tsx | xargs -I {} test {} -ge 5 && echo "PASS"</automated>
  </verify>
  <done>
    - All primary/secondary/outline/compact button variants use #84CC16 border
    - Link variant retains #76b900 text color
    - Light variant retains #76b900 hover background
  </done>
</task>

</tasks>

<verification>
- [ ] Primary button border is lime (#84CC16)
- [ ] No #76b900 borders remain in button variants
- [ ] Link and light variants retain data-green where appropriate
</verification>

<success_criteria>
- grep confirms #84CC16 in border classes
- grep confirms #76b900 removed from border classes
- Visual: primary buttons have lime border, not green
</success_criteria>

<output>
After completion, create `.planning/phases/13-nvidia-ui-redesign/13-06-SUMMARY.md`
</output>