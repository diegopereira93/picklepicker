---
phase: 13-nvidia-ui-redesign
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/footer.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/paddles/page.tsx
  - frontend/src/components/ui/card.tsx
autonomous: true
gap_closure: true
requirements:
  - HY-11
must_haves:
  truths:
    - "All components use hy-* class prefix instead of nv-*"
    - "Class migration complete across footer, home page, catalog, and card components"
  artifacts:
    - path: "frontend/src/components/layout/footer.tsx"
      provides: "Footer with hy-* classes"
      contains: "hy-footer"
    - path: "frontend/src/app/page.tsx"
      provides: "Home page with hy-* classes"
      contains: "hy-dark-section"
    - path: "frontend/src/app/paddles/page.tsx"
      provides: "Catalog page with hy-* classes"
      contains: "hy-catalog-grid"
    - path: "frontend/src/components/ui/card.tsx"
      provides: "Card component with hy-* classes"
      contains: "hy-card"
  key_links:
    - from: "All component files"
      to: "globals.css"
      via: "className hy-* classes"
      pattern: "hy-"
---

<objective>
Migrate all remaining nv-* classes to hy-* classes in footer, home page, catalog, and card components.

Purpose: HY-11 requires complete migration from nv-* to hy-* class prefixes. While CSS aliases make nv-* functional, the migration should be complete to remove CSS bloat and finalize the Hybrid design system adoption.
Output: All 4 files with hy-* class names instead of nv-*.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-nvidia-ui-redesign/CONTEXT.md

<interfaces>
<!-- Class migration mapping from CONTEXT.md HY-11 -->
nv-dark-section -> hy-dark-section
nv-light-section -> hy-light-section
nv-container -> hy-container
nv-section -> hy-section
nv-section-hero -> hy-section-hero
nv-section-label -> hy-section-label
nv-product-card -> hy-product-card
nv-catalog-grid -> hy-catalog-grid
nv-nav -> hy-nav
nv-nav-link -> hy-nav-link
nv-card -> hy-card
nv-footer -> hy-footer (add if not in globals.css)
nv-hero -> hy-hero
nv-display -> hy-display
nv-subheading -> hy-subheading
nv-section-heading -> hy-section-heading
nv-card-title-text -> hy-card-title-text
nv-card-description -> hy-card-description
nv-card-header -> hy-card-header
nv-card-content -> hy-card-content
nv-card-footer -> hy-card-footer
nv-product-card-inner -> hy-product-card-inner
nv-product-card-title -> hy-product-card-title
nv-product-card-brand -> hy-product-card-brand
nv-product-card-price -> hy-product-card-price
nv-product-image -> hy-product-image
nv-skill-badge -> hy-skill-badge
nv-specs-row -> hy-specs-row
nv-stock-in -> hy-stock-in
nv-stock-out -> hy-stock-out
nv-catalog-header -> hy-catalog-header
nv-breadcrumb -> hy-breadcrumb
nv-body -> hy-body
nv-link-dark -> hy-link-dark
nv-caption-small -> hy-caption-small
nv-footer-grid -> hy-footer-grid
nv-skeleton-card -> hy-skeleton-card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate footer.tsx to hy-* classes</name>
  <files>frontend/src/components/layout/footer.tsx</files>
  <read_first>
    - frontend/src/components/layout/footer.tsx (current state with nv-* classes)
    - frontend/src/app/globals.css (verify hy-* class definitions)
  </read_first>
  <behavior>
    - All nv-* classes replaced with hy-* equivalents
    - Footer renders identically (CSS aliases ensure no visual change)
  </behavior>
  <action>
    Modify frontend/src/components/layout/footer.tsx:

    Replace ALL nv-* class names with hy-* equivalents:
    - `nv-footer` -> `hy-footer`
    - `nv-dark-section` -> `hy-dark-section`
    - `nv-container` -> `hy-container`
    - `nv-caption-small` -> `hy-caption-small`
    - `nv-footer-grid` -> `hy-footer-grid`
    - `nv-nav-brand` -> `hy-nav-brand`
    - `nv-link-dark` -> `hy-link-dark`

    Use global find/replace or sed to ensure all occurrences are updated.
  </action>
  <acceptance_criteria>
    - grep 'nv-' frontend/src/components/layout/footer.tsx returns ZERO matches
    - grep 'hy-' frontend/src/components/layout/footer.tsx returns at least 10 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'nv-' frontend/src/components/layout/footer.tsx | xargs -I {} test {} -eq 0 && echo "PASS"</automated>
  </verify>
  <done>
    - All nv-* classes replaced with hy-*
    - Footer structure unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate page.tsx (home) to hy-* classes</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>
    - frontend/src/app/page.tsx (current state with nv-* classes)
    - frontend/src/app/globals.css (verify hy-* class definitions)
  </read_first>
  <behavior>
    - All nv-* classes replaced with hy-* equivalents
    - Home page renders identically (CSS aliases ensure no visual change)
  </behavior>
  <action>
    Modify frontend/src/app/page.tsx:

    Replace ALL nv-* class names with hy-* equivalents:
    - `nv-dark-section` -> `hy-dark-section`
    - `nv-light-section` -> `hy-light-section`
    - `nv-hero` -> `hy-hero`
    - `nv-section-hero` -> `hy-section-hero`
    - `nv-container` -> `hy-container`
    - `nv-display` -> `hy-display`
    - `nv-subheading` -> `hy-subheading`
    - `nv-section` -> `hy-section`
    - `nv-section-label` -> `hy-section-label`
    - `nv-section-heading` -> `hy-section-heading`
    - `nv-card` -> `hy-card`
    - `nv-card-title-text` -> `hy-card-title-text`
    - `nv-card-description` -> `hy-card-description`

    Use global find/replace or sed to ensure all occurrences are updated.
  </action>
  <acceptance_criteria>
    - grep 'nv-' frontend/src/app/page.tsx returns ZERO matches
    - grep 'hy-' frontend/src/app/page.tsx returns at least 15 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'nv-' frontend/src/app/page.tsx | xargs -I {} test {} -eq 0 && echo "PASS"</automated>
  </verify>
  <done>
    - All nv-* classes replaced with hy-*
    - Home page structure unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate paddles/page.tsx (catalog) to hy-* classes</name>
  <files>frontend/src/app/paddles/page.tsx</files>
  <read_first>
    - frontend/src/app/paddles/page.tsx (current state with nv-* classes)
    - frontend/src/app/globals.css (verify hy-* class definitions)
  </read_first>
  <behavior>
    - All nv-* classes replaced with hy-* equivalents
    - Catalog page renders identically (CSS aliases ensure no visual change)
  </behavior>
  <action>
    Modify frontend/src/app/paddles/page.tsx:

    Replace ALL nv-* class names with hy-* equivalents:
    - `nv-dark-section` -> `hy-dark-section`
    - `nv-container` -> `hy-container`
    - `nv-catalog-header` -> `hy-catalog-header`
    - `nv-breadcrumb` -> `hy-breadcrumb`
    - `nv-section-label` -> `hy-section-label`
    - `nv-section-heading` -> `hy-section-heading`
    - `nv-body` -> `hy-body`
    - `nv-catalog-grid` -> `hy-catalog-grid`
    - `nv-product-card` -> `hy-product-card`
    - `nv-product-card-inner` -> `hy-product-card-inner`
    - `nv-product-image` -> `hy-product-image`
    - `nv-product-card-title` -> `hy-product-card-title`
    - `nv-product-card-brand` -> `hy-product-card-brand`
    - `nv-skill-badge` -> `hy-skill-badge`
    - `nv-specs-row` -> `hy-specs-row`
    - `nv-stock-in` -> `hy-stock-in`
    - `nv-stock-out` -> `hy-stock-out`
    - `nv-product-card-price` -> `hy-product-card-price`

    Use global find/replace or sed to ensure all occurrences are updated.
  </action>
  <acceptance_criteria>
    - grep 'nv-' frontend/src/app/paddles/page.tsx returns ZERO matches
    - grep 'hy-' frontend/src/app/paddles/page.tsx returns at least 20 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'nv-' frontend/src/app/paddles/page.tsx | xargs -I {} test {} -eq 0 && echo "PASS"</automated>
  </verify>
  <done>
    - All nv-* classes replaced with hy-*
    - Catalog page structure unchanged
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate card.tsx to hy-* classes</name>
  <files>frontend/src/components/ui/card.tsx</files>
  <read_first>
    - frontend/src/components/ui/card.tsx (current state with nv-* classes)
    - frontend/src/app/globals.css (verify hy-* class definitions)
  </read_first>
  <behavior>
    - All nv-* classes replaced with hy-* equivalents
    - Card component renders identically (CSS aliases ensure no visual change)
  </behavior>
  <action>
    Modify frontend/src/components/ui/card.tsx:

    Replace ALL nv-* class names with hy-* equivalents:
    - `nv-card` -> `hy-card`
    - `nv-card-header` -> `hy-card-header`
    - `nv-card-title-text` -> `hy-card-title-text`
    - `nv-card-description` -> `hy-card-description`
    - `nv-card-content` -> `hy-card-content`
    - `nv-card-footer` -> `hy-card-footer`

    Use global find/replace or sed to ensure all occurrences are updated.
  </action>
  <acceptance_criteria>
    - grep 'nv-' frontend/src/components/ui/card.tsx returns ZERO matches
    - grep 'hy-' frontend/src/components/ui/card.tsx returns at least 6 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'nv-' frontend/src/components/ui/card.tsx | xargs -I {} test {} -eq 0 && echo "PASS"</automated>
  </verify>
  <done>
    - All nv-* classes replaced with hy-*
    - Card component structure unchanged
  </done>
</task>

</tasks>

<verification>
- [ ] No nv-* classes remain in footer.tsx
- [ ] No nv-* classes remain in page.tsx
- [ ] No nv-* classes remain in paddles/page.tsx
- [ ] No nv-* classes remain in card.tsx
- [ ] All hy-* classes correctly defined in globals.css
</verification>

<success_criteria>
- grep confirms zero nv-* matches across all 4 files
- Visual: all pages render identically (CSS aliases worked during migration)
</success_criteria>

<output>
After completion, create `.planning/phases/13-nvidia-ui-redesign/13-08-SUMMARY.md`
</output>