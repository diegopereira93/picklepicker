---
phase: 10-performance-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/app/globals.css
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Skip-to-content link is first focusable element when pressing Tab"
    - "Skip link is visually hidden but accessible to screen readers"
    - "Skip link becomes visible on focus"
    - "Activating skip link moves focus to main content"
  artifacts:
    - path: "frontend/src/app/layout.tsx"
      provides: "Skip-to-content link in body"
      min_lines: 30
    - path: "frontend/src/app/globals.css"
      provides: ".sr-only utility class"
      contains: ".sr-only"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "#main-content"
      via: "href anchor link"
      pattern: "href=\"#main-content\""
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/app/globals.css"
      via: "CSS class import"
      pattern: "sr-only"
---

<objective>
Add skip-to-content link for WCAG 2.1 AA keyboard navigation compliance.

Purpose: Enable keyboard users to bypass navigation and jump directly to main content.
Output: Skip link component + sr-only CSS utility class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-performance-ux-polish/10-VERIFICATION.md
@frontend/src/app/layout.tsx
@frontend/src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add sr-only utility class to globals.css</name>
  <files>frontend/src/app/globals.css</files>
  <action>
    Add .sr-only utility class for screen reader-only content. This class visually hides content while keeping it accessible to screen readers.

    Add after existing utility classes in globals.css:
    ```css
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    .sr-only:focus {
      position: static;
      width: auto;
      height: auto;
      padding: inherit;
      margin: inherit;
      overflow: visible;
      clip: auto;
      white-space: normal;
    }
    ```

    Gap reason: VERIFICATION.md line 108 — ".sr-only utility class not defined in globals.css"
  </action>
  <verify>
    <automated>grep -c "\.sr-only" frontend/src/app/globals.css</automated>
  </verify>
  <done>.sr-only class exists with focus state for visibility</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add skip-to-content link in layout.tsx</name>
  <files>frontend/src/app/layout.tsx</files>
  <action>
    Add skip-to-content link as the first focusable element inside the body, immediately after the opening body tag (or as first child of the root component).

    The skip link should:
    1. Use the .sr-only class for hidden state
    2. Add a focus-visible variant to show on keyboard focus
    3. Link to #main-content anchor
    4. Have descriptive text: "Pular para o conteúdo principal"

    In layout.tsx, modify the body/root structure:
    ```tsx
    <body className={inter.className}>
      <a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-primary focus:text-primary-foreground focus:rounded-md">
        Pular para o conteúdo principal
      </a>
      {/* ... rest of layout ... */}
    </body>
    ```

    Also add id="main-content" to the main wrapper element that contains the page content (likely the div or main element that wraps children).

    Gap reason: VERIFICATION.md line 9-12 — "Missing skip-to-content link for keyboard navigation"
  </action>
  <verify>
    <automated>grep -c "href=\"#main-content\"" frontend/src/app/layout.tsx</automated>
  </verify>
  <done>Skip link exists as first focusable element, links to #main-content</done>
</task>

</tasks>

<verification>
- [ ] .sr-only class defined in globals.css
- [ ] Skip link is first focusable element in layout.tsx
- [ ] Skip link targets #main-content
- [ ] Main content wrapper has id="main-content"
</verification>

<success_criteria>
- Keyboard users can press Tab once to reach skip link
- Screen readers announce skip link
- Activating skip link moves focus to main content
- WCAG 2.1 AA skip link requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-ux-polish/10-02-SUMMARY.md`
</output>
