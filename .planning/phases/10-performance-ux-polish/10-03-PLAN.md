---
phase: 10-performance-ux-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - package.json
  - frontend/package.json
autonomous: false
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Lighthouse audit runs successfully"
    - "Core Web Vitals metrics are measured and reported"
    - "Performance thresholds are verified (LCP < 2.5s, CLS < 0.1, FID < 100ms)"
  artifacts:
    - path: "package.json"
      provides: "Lighthouse npm script"
      contains: "lighthouse"
    - path: "frontend/package.json"
      provides: "Lighthouse script or chrome-launcher dependency"
      contains: "lighthouse"
  key_links:
    - from: "package.json script"
      to: "running application"
      via: "npm run lighthouse command"
      pattern: "lighthouse.*http"
---

<objective>
Run Lighthouse audit to verify Core Web Vitals thresholds are met.

Purpose: Measure and validate LCP, CLS, FID against target thresholds.
Output: Lighthouse report with performance scores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-performance-ux-polish/10-VERIFICATION.md
@package.json
@frontend/package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Lighthouse npm script</name>
  <files>package.json</files>
  <action>
    Add a Lighthouse audit script to package.json (root or frontend level).

    Option A - Using lighthouse npm package:
    1. Install: `npm install -D lighthouse`
    2. Add script: `"lighthouse": "lighthouse http://localhost:3000 --output html --output-path ./lighthouse-report.html --chrome-flags='--headless'"`

    Option B - Using Chrome CLI (if lighthouse globally installed):
    Add script: `"lighthouse": "lighthouse http://localhost:3000 --output json --output-path ./lighthouse-report.json"`

    The script should:
    - Target the running application at localhost:3000
    - Output a report (HTML or JSON)
    - Run in headless mode for CI compatibility

    Gap reason: VERIFICATION.md line 17-20 — "No Lighthouse audit has been run to verify the thresholds"
  </action>
  <verify>
    <automated>grep -c "lighthouse" package.json || grep -c "lighthouse" frontend/package.json</automated>
  </verify>
  <done>Lighthouse script added to package.json</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Lighthouse npm script added to package.json for automated Core Web Vitals auditing.
  </what-built>
  <how-to-verify>
    1. Start the development server: `npm run dev` (in frontend directory)
    2. Wait for server to be ready at http://localhost:3000
    3. Run the lighthouse audit: `npm run lighthouse`
    4. Open the generated report (lighthouse-report.html or lighthouse-report.json)
    5. Verify the following thresholds:
       - Performance score >= 90
       - LCP (Largest Contentful Paint) < 2.5s
       - CLS (Cumulative Layout Shift) < 0.1
       - FID (First Input Delay) < 100ms
    6. If thresholds not met, note which pages need optimization
  </how-to-verify>
  <resume-signal>Type "approved" with scores or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] Lighthouse script exists in package.json
- [ ] Lighthouse audit runs successfully
- [ ] Report generated with Core Web Vitals metrics
- [ ] All thresholds verified (LCP < 2.5s, CLS < 0.1, FID < 100ms)
</verification>

<success_criteria>
- Lighthouse audit completes without errors
- Performance score >= 90
- All Core Web Vitals thresholds met
- Gap from VERIFICATION.md closed
</success_criteria>

<output>
After completion, create `.planning/phases/10-performance-ux-polish/10-03-SUMMARY.md`
</output>
