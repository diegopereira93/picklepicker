---
phase: 28-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/gift/page.tsx
  - frontend/src/app/gift/results/page.tsx
autonomous: true
requirements:
  - FR-01

must_haves:
  truths:
    - "Gift page (/gift) renders with dark theme background and styled buttons"
    - "Gift results page (/gift/results) renders with dark theme and styled buttons"
    - "Zero wg-* CSS classes remain in gift page files"
    - "Zero var(--warm-white) references remain"
    - "All buttons use shadcn Button component or Tailwind dark-theme utilities"
    - "Existing gift quiz flow (welcome → recipient → budget → analyzing → results) still works end-to-end"
  artifacts:
    - path: "frontend/src/app/gift/page.tsx"
      provides: "Gift quiz flow with dark theme"
      contains: "bg-base"
    - path: "frontend/src/app/gift/results/page.tsx"
      provides: "Gift results display with dark theme"
      contains: "bg-base"
  key_links:
    - from: "frontend/src/app/gift/page.tsx"
      to: "Button component"
      via: "import from @/components/ui/button"
      pattern: "from '@/components/ui/button'"
    - from: "frontend/src/app/gift/page.tsx"
      to: "Tailwind design tokens"
      via: "className utilities"
      pattern: "bg-base|text-text-primary|text-brand-secondary"
---

<objective>
Replace all legacy CSS classes and light-theme colors in Gift pages with current dark-theme Tailwind tokens and shadcn Button components.

Purpose: Gift page (/gift) and Gift results (/gift/results) are completely broken — using non-existent wg-* CSS classes and light-theme colors that don't exist in the current dark-only design system. Pages render with no styling at all.

Output: Two fully restyled pages that match the working catalog and chat pages' dark theme.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-critical-bug-fixes/28-RESEARCH.md

<interfaces>
<!-- Design tokens from tailwind.config.ts -->
Background: base=#0a0a0a, surface=#141414, elevated=#1f1f1f
Brand: primary=#84CC16 (lime green), secondary=#F97316 (orange)
Text: primary=#FAFAFA, secondary=#A3A3A3, muted=#737373
Font: font-display (Bebas Neue), font-sans (Source Sans 3), font-mono (JetBrains Mono)
Border radius: rounded-rounded, rounded-sharp

<!-- Button variants from @/components/ui/button.tsx -->
Available variants: default, outline, ghost, destructive, link
Available sizes: xs, sm, default, lg, icon, icon-xs

<!-- From @/lib/api.ts -->
export function fetchPaddles(params?: Record<string, string>): Promise<Paddle[]>

<!-- From @/types/paddle.ts -->
export interface Paddle {
  id: number
  name: string
  brand?: string
  model_slug?: string
  image_url?: string
  price_min_brl?: number
  // ... more fields
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Gift page with dark theme</name>
  <files>frontend/src/app/gift/page.tsx</files>
  <read_first>
    - frontend/src/app/gift/page.tsx
    - frontend/src/app/catalog/[slug]/page.tsx (reference: working dark theme page)
    - frontend/src/components/ui/button.tsx (Button component API)
  </read_first>
  <action>
Replace ALL legacy styling in gift/page.tsx with current dark-theme patterns:

1. **Add Button import** at top: `import { Button } from '@/components/ui/button'`

2. **Replace ALL backgrounds:**
   - `bg-[var(--warm-white)]` (6 instances across welcome/recipient/budget/analyzing steps) → `bg-base`
   - Remove `aria-label` from wrapping `<main>` if it causes layout issues

3. **Replace wg-button-coral** (1 instance, line ~162, budget step "Encontrar presente" button):
   Replace the raw `<button>` with:
   ```tsx
   <Button
     onClick={handleBudgetSubmit}
     disabled={budget === 0}
     size="lg"
     className="w-full max-w-xs bg-brand-secondary hover:bg-brand-secondary/80 text-white text-lg"
   >
     Encontrar presente
   </Button>
   ```

4. **Replace the recommendation result buttons** (wg-button-coral and wg-button-ghost in the results section):
   - "VER MAIS OPÇÕES →" → `<Button size="lg" className="bg-brand-secondary hover:bg-brand-secondary/80 text-white">Ver mais opções</Button>`
   - "Quero fazer o quiz pra mim" → `<Button variant="outline" size="lg">Quero fazer o quiz pra mim</Button>`
   - "Refazer" → `<Button variant="ghost" size="lg">Refazer</Button>`

5. **Replace hardcoded light-theme colors in results section:**
   - `text-coral` → `text-brand-secondary`
   - `text-gray-600` → `text-text-secondary`
   - `bg-yellow-50` → `bg-elevated`
   - `text-green-700` → `text-brand-primary`

6. **Keep ALL existing logic intact** — quiz flow steps, state management, fetchPaddles call, formatCurrency helper, recipient level labels. Only change styling classes.
  </action>
  <acceptance_criteria>
    - `gift/page.tsx` contains zero occurrences of `wg-` string
    - `gift/page.tsx` contains zero occurrences of `var(--warm-white)`
    - `gift/page.tsx` contains zero occurrences of `text-coral` or `bg-yellow-50`
    - `gift/page.tsx` contains `bg-base` for main backgrounds
    - `gift/page.tsx` imports `Button` from `@/components/ui/button`
    - `gift/page.tsx` uses `text-brand-secondary` or `text-brand-primary` instead of hardcoded hex colors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Gift page renders with dark theme, all wg-* classes replaced, quiz flow still works, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Gift results page with dark theme</name>
  <files>frontend/src/app/gift/results/page.tsx</files>
  <read_first>
    - frontend/src/app/gift/results/page.tsx
    - frontend/src/components/ui/button.tsx (Button component API)
  </read_first>
  <action>
Replace ALL legacy styling in gift/results/page.tsx with dark-theme patterns:

1. **Add Button import** at top: `import { Button } from '@/components/ui/button'`

2. **Replace backgrounds:**
   - Any `bg-[var(--warm-white)]` or light background → `bg-base` for main wrapper
   - Card backgrounds → `bg-surface rounded-lg p-4` or `bg-surface rounded-lg shadow-md`

3. **Replace wg-button-coral** (1 instance, "VER MAIS OPÇÕES →"):
   Replace raw `<button>` with:
   ```tsx
   <Button
     onClick={() => { window.location.href = '/catalog' }}
     size="lg"
     className="bg-brand-secondary hover:bg-brand-secondary/80 text-white"
   >
     Ver mais opções
   </Button>
   ```

4. **Replace wg-button-ghost** (2 instances):
   - "Quero fazer o quiz pra mim" → `<Button variant="outline" size="lg" onClick={() => { window.location.href = '/quiz' }}>Quero fazer o quiz pra mim</Button>`
   - "Refazer" → `<Button variant="ghost" size="lg" onClick={() => { window.location.href = '/gift' }}>Refazer</Button>`

5. **Replace hardcoded colors in the recommendation card:**
   - `text-coral` → `text-brand-secondary`
   - `text-gray-600` → `text-text-secondary`
   - `bg-yellow-50` → `bg-elevated`
   - `text-green-700` → `text-brand-primary`
   - Light text on light bg → `text-text-primary` and `text-text-secondary`
   - Image placeholder bg → `bg-elevated`

6. **Keep ALL existing logic** — fetchPaddles call, recommendation rendering, gift results display.
  </action>
  <acceptance_criteria>
    - `gift/results/page.tsx` contains zero occurrences of `wg-` string
    - `gift/results/page.tsx` contains `bg-base` for main background
    - `gift/results/page.tsx` imports `Button` from `@/components/ui/button`
    - `gift/results/page.tsx` uses `text-brand-secondary` instead of `text-coral`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c 'wg-' src/app/gift/results/page.tsx</automated>
  </verify>
  <done>Gift results page renders with dark theme, all wg-* classes replaced, all tests pass</done>
</task>

</tasks>

<verification>
1. Run frontend tests: `cd frontend && npx vitest run`
2. Verify zero wg- classes: `grep -rn 'wg-' src/app/gift/ --include='*.tsx'` should return nothing
3. Verify zero var(--warm-white): `grep -rn 'warm-white' src/app/gift/` should return nothing
4. Verify Button import: `grep 'from.*@/components/ui/button' src/app/gift/page.tsx src/app/gift/results/page.tsx`
</verification>

<success_criteria>
- Both gift pages render with dark theme styling
- Zero wg-* CSS classes remain in either file
- Zero var(--warm-white) references remain
- All 179 frontend tests pass
- Gift quiz flow (welcome → recipient → budget → results) works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/28-critical-bug-fixes/28-01-SUMMARY.md`
</output>
