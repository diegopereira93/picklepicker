---
phase: 28-critical-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/product-card.tsx
autonomous: true
requirements:
  - FR-14

must_haves:
  truths:
    - "Product card 'Details' button shows 'Detalhes' (Portuguese)"
    - "Product card 'Compare' button shows 'Comparar' (Portuguese)"
    - "No English user-facing strings remain in product-card.tsx"
    - "No English user-facing strings in any UI component"
  artifacts:
    - path: "frontend/src/components/ui/product-card.tsx"
      provides: "Product card with Portuguese button labels"
      contains: "Detalhes"
  key_links:
    - from: "frontend/src/components/ui/product-card.tsx"
      to: "ProductCard component"
      via: "Button text"
      pattern: "Detalhes|Comparar"
---

<objective>
Fix all remaining English strings in user-facing UI components, starting with the known "Details" button on product cards.

Purpose: The product card "Details" button is the only visible English text in the entire UI. All other text is Portuguese. This creates a jarring inconsistency. Additionally, "Compare" button text should also be translated.

Output: Zero English strings in any user-facing component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-critical-bug-fixes/28-RESEARCH.md

<interfaces>
<!-- From @/components/ui/product-card.tsx — current English strings -->
// Line 156: "Details"  → must change to "Detalhes"
// Line 165: "Compare"  → must change to "Comparar"

// Button already uses shadcn Button component — only text content changes needed
// No structural or styling changes required
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix English strings in product card</name>
  <files>frontend/src/components/ui/product-card.tsx</files>
  <read_first>
    - frontend/src/components/ui/product-card.tsx
  </read_first>
  <action>
Change exactly two English strings to Portuguese:

1. **Line ~156:** Change button text from `Details` to `Detalhes`
   ```tsx
   // FROM:
   Details
   // TO:
   Detalhes
   ```

2. **Line ~165:** Change button text from `Compare` to `Comparar`
   ```tsx
   // FROM:
   Compare
   // TO:
   Comparar
   ```

Do NOT change any other code — no className changes, no import changes, no structural changes. Only the two string literals.
  </action>
  <acceptance_criteria>
    - `product-card.tsx` contains `"Detalhes"` (NOT `"Details"`)
    - `product-card.tsx` contains `"Comparar"` (NOT standalone `"Compare"`)
    - `product-card.tsx` has no other changes beyond the two string replacements
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c 'Details' src/components/ui/product-card.tsx</automated>
  </verify>
  <done>Product card shows "Detalhes" and "Comparar" instead of English text</done>
</task>

<task type="auto">
  <name>Task 2: Scan and fix all remaining English strings in UI components</name>
  <files>frontend/src/components/**/*.tsx (read-only scan, fix as needed)</files>
  <read_first>
    - frontend/src/components/ui/product-card.tsx (verify Task 1 fix applied)
  </read_first>
  <action>
Scan ALL component files under `frontend/src/components/` and `frontend/src/app/` for remaining English user-facing strings.

**How to identify English user-facing strings:**
- Look for string literals inside JSX: `<span>text</span>`, `<p>text</p>`, `<Button>text</Button>`
- Look for string literals in labels, titles, descriptions, placeholders
- Look for English words in template expressions that render to users

**How to identify NON-user-facing strings (do NOT change):**
- Variable names, function names, CSS class names
- `aria-label` values (accessibility, not visible)
- Import paths, type names
- Console.log messages
- API endpoint paths
- Error codes, status codes
- `alt` text on images (can be translated but lower priority)

**Known areas to check:**
- `frontend/src/components/ui/compare-row.tsx` — compare table labels
- `frontend/src/components/ui/price-alert-modal.tsx` — already Portuguese ("Alerta de Preco", "Criar Alerta")
- `frontend/src/components/home/` — landing page content
- `frontend/src/app/` page files — page-specific headings and text

**Expected findings:**
- Most components should already be Portuguese (based on scan in RESEARCH.md)
- `quiz-profile.ts` has English labels in `getProfileSummary()` but those are internal/developer-facing
- If no additional English user-facing strings found, just confirm the scan was done

For each English string found, replace with Portuguese equivalent. Use consistent, natural PT-BR phrasing.
  </action>
  <acceptance_criteria>
    - Grep for common English UI words returns zero user-facing hits: `grep -rn '"Details"\|"Compare"\|"Submit"\|"Cancel"\|"Save"\|"Delete"\|"Edit"\|"Close"\|"Open"\|"Loading"' frontend/src/components/ --include='*.tsx'` (after excluding aria-labels and internal strings)
    - Any newly found English strings replaced with PT-BR equivalents
    - All 179 frontend tests pass
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Zero English user-facing strings in all UI components, all tests pass</done>
</task>

</tasks>

<verification>
1. Run frontend tests: `cd frontend && npx vitest run`
2. Verify product card: `grep 'Detalhes' src/components/ui/product-card.tsx` returns match
3. Verify no English in product card: `grep '"Details"' src/components/ui/product-card.tsx` returns nothing
4. Verify no English in product card: `grep 'Compare' src/components/ui/product-card.tsx | grep -v 'onCompare\|import\|GitCompare\|compareRow\|CompareRow'` returns nothing
5. Full scan: `grep -rn '"Details"\|"Compare"\|"Submit"\|"Cancel"' src/components/ --include='*.tsx'` returns no user-facing hits
</verification>

<success_criteria>
- Product card "Details" → "Detalhes"
- Product card "Compare" → "Comparar"
- Zero English user-facing strings in all components
- All 179 frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-critical-bug-fixes/28-03-SUMMARY.md`
</output>
