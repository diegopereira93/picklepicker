---
phase: 28-critical-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/quiz/results/page.tsx
  - frontend/src/app/layout.tsx
autonomous: true
requirements:
  - FR-01
  - FR-02
  - FR-03

must_haves:
  truths:
    - "Quiz results page (/quiz/results) renders with dark theme and styled components"
    - "HTML lang attribute is pt-BR"
    - "Page metadata title and description are in Portuguese"
    - "Quiz results page reads profile from same module quiz writes to (quiz-profile.ts)"
    - "Quiz → Results flow works without redirect loop"
  artifacts:
    - path: "frontend/src/app/quiz/results/page.tsx"
      provides: "Quiz results with dark theme and correct profile source"
      contains: "loadQuizProfile"
    - path: "frontend/src/app/layout.tsx"
      provides: "Root layout with pt-BR locale"
      contains: 'lang="pt-BR"'
  key_links:
    - from: "frontend/src/app/quiz/results/page.tsx"
      to: "@/lib/quiz-profile"
      via: "import loadQuizProfile"
      pattern: "from '@/lib/quiz-profile'"
    - from: "frontend/src/app/quiz/page.tsx"
      to: "frontend/src/app/quiz/results/page.tsx"
      via: "redirect after saveQuizProfile → loadQuizProfile reads same key"
      pattern: "pickleiq_player_profile"
---

<objective>
Fix the most broken page (quiz results) with dark theme migration AND fix the profile storage mismatch AND fix the HTML lang attribute — all in one plan since quiz/results is the convergence point.

Purpose: Quiz results page is the most severely broken (7+ wg-* classes, 12+ hardcoded colors, wrong profile import). Layout has wrong lang attribute. Both are P0 blockers.

Output: Dark-themed quiz results page that correctly reads quiz profile data, and root layout with pt-BR locale.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-critical-bug-fixes/28-RESEARCH.md

<interfaces>
<!-- Design tokens from tailwind.config.ts -->
Background: base=#0a0a0a, surface=#141414, elevated=#1f1f1f
Brand: primary=#84CC16 (lime green), secondary=#F97316 (orange)
Text: primary=#FAFAFA, secondary=#A3A3A3, muted=#737373
Font: font-display (Bebas Neue), font-sans (Source Sans 3), font-mono (JetBrains Mono)

<!-- Button variants from @/components/ui/button.tsx -->
Available variants: default, outline, ghost, destructive, link
Available sizes: xs, sm, default, lg, icon, icon-xs

<!-- From @/lib/quiz-profile.ts — THE CORRECT MODULE -->
export interface QuizProfile {
  level: 'beginner' | 'intermediate' | 'advanced' | 'competitive'
  style: 'baseline-grinder' | 'dink-control' | 'power-hitter' | 'all-round'
  priority: 'control' | 'power' | 'spin' | 'speed'
  budget: 'under-80' | '80-150' | '150-250' | '250-plus'
  weightPreference: 'light' | 'medium' | 'heavy' | 'no-preference'
  location: 'indoor' | 'outdoor' | 'both'
  targetPaddle?: string
  completedAt: string
}
export function saveQuizProfile(profile: QuizProfile): void
export function loadQuizProfile(): QuizProfile | null
export function clearQuizProfile(): void
export function hasQuizProfile(): boolean

<!-- From @/lib/profile.ts — WRONG MODULE (do not use in quiz/results) -->
Storage key: pickleiq:profile:{uid} (different from quiz-profile's pickleiq_player_profile)
export function getProfile(): Promise<UserProfile | null>  // note: ASYNC
export function saveProfile(profile: UserProfile): Promise<void>
export function clearProfile(): void

<!-- From @/types/paddle.ts -->
export interface UserProfile {
  level: string
  style: string
  budget_max: number
  identity?: string
  pain_points?: string[]
  frequency?: string
}

<!-- From @/lib/api.ts -->
export function fetchPaddles(params?: Record<string, string>): Promise<Paddle[]>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Quiz Results page with dark theme and fix profile import</name>
  <files>frontend/src/app/quiz/results/page.tsx</files>
  <read_first>
    - frontend/src/app/quiz/results/page.tsx
    - frontend/src/app/quiz/page.tsx (reference: where quiz saves profile via saveQuizProfile)
    - frontend/src/lib/quiz-profile.ts (THE CORRECT profile module to import)
    - frontend/src/components/ui/button.tsx (Button component API)
  </read_first>
  <action>
This is the most severely broken page. Complete rewrite of styling + fix critical profile import bug.

**PART A: Fix Profile Import (FR-03 — the redirect loop bug)**

1. Replace import line:
   ```typescript
   // REMOVE THIS:
   import { getProfile, clearProfile } from '@/lib/profile'
   // REPLACE WITH:
   import { loadQuizProfile, clearQuizProfile, type QuizProfile } from '@/lib/quiz-profile'
   ```

2. Replace `UserProfile` type usage with `QuizProfile`:
   - Change `const [profile, setProfile] = useState<UserProfile | null>(null)` → `useState<QuizProfile | null>(null)`

3. Fix the profile loading in useEffect:
   ```typescript
   // REMOVE:
   const storedProfile = getProfile()  // this is ASYNC and reads wrong key!
   
   // REPLACE WITH:
   const storedProfile = loadQuizProfile()  // synchronous, reads correct key
   ```
   Remove any `.then()` chain since `loadQuizProfile()` is synchronous (not async like `getProfile()`).

4. Fix clearProfile call:
   ```typescript
   // REMOVE:
   clearProfile()
   // REPLACE WITH:
   clearQuizProfile()
   ```

5. Update the profile field references in the template. QuizProfile has different field names than UserProfile:
   - `profile.budget_max` → need to convert QuizProfile budget enum to a display value
   - Budget enum values: 'under-80', '80-150', '150-250', '250-plus'
   - Create a budget display map: `const budgetLabels: Record<string, string> = { 'under-80': 'Até R$ 80', '80-150': 'R$ 80-150', '150-250': 'R$ 150-250', '250-plus': 'R$ 250+' }`
   - `profile.level` — same field name, but QuizProfile uses 'beginner'|'intermediate'|'advanced'|'competitive' (NOT the UserProfile values like 'beginner'|'regular'|'serious')
   - Update levelLabels map to match QuizProfile enum values
   - `profile.style` — QuizProfile uses 'baseline-grinder'|'dink-control'|'power-hitter'|'all-round' (NOT 'control'|'power'|'balanced')
   - Update styleLabels map accordingly

**PART B: Replace ALL Legacy Styling (FR-01)**

6. Add imports:
   ```typescript
   import { Button } from '@/components/ui/button'
   ```

7. Replace ALL wg-* CSS classes with Tailwind dark-theme equivalents:

   | What | Replace With |
   |------|-------------|
   | `wg-container` | `container mx-auto px-4` |
   | `wg-profile-summary` | `bg-surface rounded-lg p-6 border border-border` |
   | `wg-recommendation-card wg-animate-fade-up` | `bg-surface rounded-lg p-4 shadow-md border border-border` |
   | `wg-why-matches` | `bg-elevated rounded-lg p-3 mt-2` |
   | `wg-button-coral` (with inline classes) | Replace raw `<Link className="wg-button-coral ...">` with `<Button asChild className="bg-brand-secondary hover:bg-brand-secondary/80 text-white"><Link>...</Link></Button>` |
   | `wg-button-ghost` | `<Button asChild variant="ghost"><Link>...</Link></Button>` |
   | `wg-button-outline` | `<Button asChild variant="outline"><Link>...</Link></Button>` |

8. Replace ALL hardcoded hex colors:
   - `text-[#2A2A2A]` → `text-text-primary`
   - `text-[#757575]` → `text-text-muted`
   - `text-[#84CC16]` → `text-brand-primary`
   - `text-[#F97316]` → `text-brand-secondary`
   - `bg-[#F5F2EB]` → `bg-elevated`
   - `bg-[#F97316]` → `bg-brand-secondary`
   - `bg-[#84CC16]` → `bg-brand-primary`
   - `bg-[#0EA5E9]` → `bg-info`
   - `hover:bg-[#EA580C]` → `hover:bg-brand-secondary/80`
   - `hover:text-[#F97316]` → `hover:text-brand-secondary`
   - `hover:bg-[#F97316]/5` → `hover:bg-brand-secondary/10`
   - `border-[#757575]` → `border-border`

9. Replace the loading spinner colors:
   - `border-b-2 border-[#F97316]` → `border-b-2 border-brand-secondary`

10. Add dark theme background to main container:
    - Wrap the entire return in a `bg-base min-h-screen` container

11. Keep ALL existing logic intact — paddle fetching, filtering, recommendation display, links.
  </action>
  <acceptance_criteria>
    - `quiz/results/page.tsx` contains `import { loadQuizProfile` from `@/lib/quiz-profile`
    - `quiz/results/page.tsx` contains ZERO occurrences of `from '@/lib/profile'`
    - `quiz/results/page.tsx` contains ZERO occurrences of `wg-` string
    - `quiz/results/page.tsx` contains ZERO occurrences of `text-[#2A2A2A]` or `text-[#757575]`
    - `quiz/results/page.tsx` contains `bg-base` or `bg-surface` for backgrounds
    - `quiz/results/page.tsx` uses `QuizProfile` type (not `UserProfile`)
    - `quiz/results/page.tsx` imports `Button` from `@/components/ui/button`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -c "wg-\|from '@/lib/profile'\|text-\[#2A2A2A\]" src/app/quiz/results/page.tsx</automated>
  </verify>
  <done>Quiz results page uses correct profile module, renders dark theme, zero legacy classes or hardcoded colors, quiz→results flow works without redirect loop</done>
</task>

<task type="auto">
  <name>Task 2: Fix root layout lang attribute and metadata</name>
  <files>frontend/src/app/layout.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx
  </read_first>
  <action>
Fix the HTML language attribute and translate metadata to Portuguese.

1. **Change lang attribute:**
   ```tsx
   // FROM:
   <html lang="en" suppressHydrationWarning className={...}>
   // TO:
   <html lang="pt-BR" suppressHydrationWarning className={...}>
   ```

2. **Update metadata to Portuguese:**
   ```typescript
   export const metadata: Metadata = {
     title: "PickleIQ — Recomendações Inteligentes de Raquetes de Pickleball",
     description: "Encontre a raquete de pickleball perfeita com IA. Compare preços, veja especificações técnicas e receba recomendações personalizadas em português.",
   };
   ```

3. **Keep everything else unchanged** — font imports, ThemeProvider, ClerkWrapper, Header, Footer, Toaster configuration. Do NOT modify any styling, component imports, or body classes.
  </action>
  <acceptance_criteria>
    - `layout.tsx` contains `lang="pt-BR"` (NOT `lang="en"`)
    - `layout.tsx` metadata title does NOT contain "AI Pickleball Paddle Advisor"
    - `layout.tsx` metadata description starts with "Encontre" (Portuguese)
    - All other layout structure unchanged (fonts, ThemeProvider, ClerkWrapper, Header, Footer)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/diego/Projetos/picklepicker/frontend && grep -E 'lang="pt-BR"|Recomendações Inteligentes|Encontre a raquete' src/app/layout.tsx</automated>
  </verify>
  <done>Root layout has lang="pt-BR", metadata title and description in Portuguese, all other layout structure preserved</done>
</task>

</tasks>

<verification>
1. Run frontend tests: `cd frontend && npx vitest run`
2. Verify zero wg- in quiz/results: `grep -rn 'wg-' src/app/quiz/results/page.tsx` returns nothing
3. Verify correct profile import: `grep "from '@/lib/quiz-profile'" src/app/quiz/results/page.tsx` returns match
4. Verify no wrong profile import: `grep "from '@/lib/profile'" src/app/quiz/results/page.tsx` returns nothing
5. Verify lang attribute: `grep 'lang="pt-BR"' src/app/layout.tsx` returns match
6. Verify Portuguese metadata: `grep 'Recomendações' src/app/layout.tsx` returns match
</verification>

<success_criteria>
- Quiz results page renders with dark theme styling
- Quiz results reads from quiz-profile.ts (same module quiz writes to)
- Quiz → Results flow works without redirect loop
- HTML lang="pt-BR" confirmed in root layout
- Metadata title and description in Portuguese
- All 179 frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/28-critical-bug-fixes/28-02-SUMMARY.md`
</output>
